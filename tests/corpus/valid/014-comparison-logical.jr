main :: () {
    a := 1;
    b := 2;

    lt := a < b;
    le := a <= b;
    gt := a > b;
    ge := a >= b;
    eq := a == b;
    ne := a != b;

    // `&&` and `||` short-circuit; `!` negates.
    both := lt && gt;
    either := lt || gt;
    negated := !lt;
}
