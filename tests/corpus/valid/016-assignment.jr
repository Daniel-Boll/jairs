main :: () {
    a := 0;

    a = 1;

    // Compound assignment. These trap on overflow exactly like their
    // binary forms.
    a += 2;
    a -= 3;
    a *= 4;
    a /= 5;
    a %= 6;

    // Wrapping compound forms.
    a +%= 7;
    a -%= 8;
    a *%= 9;
}
