// EXPECT: E0237 — an enum member value that calls a procedure (ADR-0245 §2)
//
// Arithmetic enum values now use the same VM-backed declaration evaluator as arrays, layout
// attributes, SIMD counts, and SoA counts. A call remains outside the first slice because the
// provisional program does not carry procedure routines yet.
four :: () -> s64 {
  return 4;
}

Bad :: enum {
  X :: four();
}

main :: () {
}
