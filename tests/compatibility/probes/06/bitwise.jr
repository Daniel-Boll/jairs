#import "Basic";

main :: () {
  value: u8 = 90;
  mask: u8 = 60;
  rotated := (value << 1) | (value >> 7);

  print(
    "bits % % % % % % %\n",
    value & mask,
    value | mask,
    value ^ mask,
    ~value,
    value << 1,
    value >> 2,
    rotated,
  );
}
