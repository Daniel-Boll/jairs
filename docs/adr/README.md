# Architecture Decision Records

An **Architecture Decision Record** (ADR) captures a single significant,
hard-to-reverse decision: the forces that pressed on it, the choice made, and
the consequences that choice forces onto the rest of the system. It is a record,
not a proposal — an ADR is written once the decision is made, and it is
*immutable*. If a later decision overturns an earlier one, we write a new ADR
that supersedes it rather than editing history.

Add an ADR when a decision (a) shapes the architecture, an interface, or a data
representation; (b) is expensive to undo later; and (c) a future contributor
would otherwise reasonably ask "why on earth is it done this way?". Small,
local, easily-reversed choices do not need an ADR. Everything in the table below
came out of [`PLAN.md`](../../PLAN.md) §0.1 — the resolved design questions — and
each is load-bearing for a specific crate, wave, or IR representation.

The ADR numbers are stable and are referenced from code comments and
`Cargo.toml`. **Do not renumber.**

## Index

| ADR | Title | Status |
|---|---|---|
| [0001](0001-implicit-context-hidden-parameter.md) | Implicit context is a hidden trailing parameter | Accepted |
| [0002](0002-integer-overflow-traps.md) | Integer overflow always traps | Accepted |
| [0003](0003-bounds-checks-build-setting.md) | Bounds checks are a build setting | Accepted |
| [0004](0004-string-representation.md) | Strings are `{data, count}` and not NUL-terminated | Accepted |
| [0005](0005-structural-polymorph-identity.md) | Polymorph instantiation identity is structural | Accepted |
| [0006](0006-comptime-ffi.md) | Compile-time code may call foreign functions | Accepted |
| [0007](0007-salsa-single-frontend.md) | salsa from the first slice; the LSP is a query consumer | Accepted |
| [0008](0008-error-handling-effect-row-slot.md) | Error handling is Jai's, with a reserved effect-row slot | Accepted |
| [0009](0009-pinned-cranelift-salsa.md) | `cranelift-*` and `salsa` are pinned with `=` | Accepted |
| [0010](0010-handwritten-parser-separate-treesitter.md) | Hand-written compiler parser; tree-sitter is editor-only | Accepted |
| [0011](0011-postfix-deref-prefix-address-of.md) | Dereference is postfix `.*`; address-of is prefix `*` | Accepted |
| [0012](0012-procs-and-structs-are-constants.md) | Procedures and structs are constants | Accepted |
| [0013](0013-hir-spans-defer-astidmap.md) | HIR nodes carry spans; `AstIdMap` is deferred | Accepted |
| [0014](0014-module-resolution.md) | Module resolution: search paths, flat imports, cycles are legal | Accepted |
| [0015](0015-type-identity.md) | Type identity: nominal structs, distinct `string`, interned `void` | Accepted |
| [0016](0016-jairs-0-typing-rules.md) | Jairs-0 typing rules: context-typed literals, deferred `#run` | Accepted |
| [0017](0017-mir-shape.md) | MIR shape: block parameters, SSA at construction, poison refused | Accepted |
| [0018](0018-vm-shape.md) | VM shape: register bytecode, layout in the pool, const-eval as a query | Accepted |
| [0019](0019-native-backend-shape.md) | Native back end: three-phase `Backend`, traps via a runtime helper, interned foreign library | Accepted |
| [0020](0020-trap-source-locations.md) | A trap names its source location; one formatter in `jr-base` decides how | Accepted |
| [0021](0021-inliner-and-optimized-mir.md) | The inliner, a staged `optimized_file_mir`, and the `#run` closure it must not touch | Accepted |
| [0022](0022-dce-constprop-shared-arithmetic.md) | DCE and const-prop, ADR-0002's arithmetic shared in `jr-pool`, and a bounded fixed point | Accepted |
| [0023](0023-store-to-load-forwarding.md) | Store-to-load forwarding: block-local, identical paths, and no layout | Accepted |
| [0024](0024-language-server.md) | The language server: a worker snapshot, a span scan, negotiated positions | Accepted |
| [0025](0025-editor-integration.md) | Editor integration as a runtimepath directory, verified rather than gated | Accepted |
| [0026](0026-root-marker-order.md) | `.git` before `modules` as the workspace root marker (amends ADR-0025 §1) | Accepted |
| [0027](0027-doc-comments.md) | `///` and `//!` doc comments: trivia kinds plus a side-table query | Accepted |
| [0028](0028-hover-and-completion.md) | The hover card, and completion pulled forward from W9 | Accepted |
| [0029](0029-workspace-discovery.md) | The workspace is the search paths plus the root tree, walked and watched | Accepted |
| [0030](0030-references-and-rename.md) | References, rename that refuses rather than half-renames, and symbols | Accepted |
| [0031](0031-code-actions-and-hints.md) | Code actions from diagnostics, an unused-import warning, signature help, inlay hints | Accepted |
| [0032](0032-write-before-queue.md) | Every write before the snapshot; a cancelled publish must be re-queued (amends ADR-0024 §2) | Accepted |
| [0033](0033-latency-measurement.md) | `jr bench`: latency measured in three cache regimes, because a benchmark harness would measure the memo | Accepted |
| [0034](0034-no-reverse-index.md) | No reverse index: the reference scan is 99% parsing, 1% searching (closes ADR-0030's reservation) | Accepted |
| [0035](0035-import-navigation.md) | An `#import` line navigates to its module, from anywhere on the line | Accepted |
| [0036](0036-no-vscode-extension.md) | No VS Code extension; Neovim is the supported editor (amends §1.4's first criterion) | Accepted |
| [0037](0037-numeric-tower-and-cast.md) | The integer tower is a naming change; `cast(T, x)` is Jai's, checked at comptime | Accepted |
| [0038](0038-negative-literals.md) | A leading `-` on a literal is folded during lowering, so a signed minimum is writable | Accepted |
| [0039](0039-fixed-arrays-and-bounds-checks.md) | `[N]T` fixed arrays, and the explicit `bounds_check` op ADR-0003 asked for in the slice | Accepted |
| [0040](0040-floating-point.md) | `float32`/`float64` are plain IEEE-754 with no traps; `Convert` carries a `NumKind` | Accepted |
| [0041](0041-enums.md) | `enum` is a nominal type whose members are namespaced; bare `.RED` is deferred with a plan | Accepted |
| [0042](0042-bitwise-operators.md) | Bitwise binds tighter than comparison (unlike C); an out-of-range shift traps | Accepted |
| [0043](0043-enum-flags.md) | `enum_flags` numbers by powers of two; a combination is a value, not a member | Accepted |
| [0044](0044-array-views.md) | `[]T` is a `{data, count}` pair; an array converts to one only via explicit `buf[]` | Accepted |
| [0045](0045-unions.md) | `union` is untagged — a tag with no pattern matching to read it is cost without benefit | Accepted |
| [0046](0046-autocast-and-bare-enum-members.md) | `xx` and bare `.RED` are one idea: the context supplies what the source omits | Accepted |
| [0047](0047-imported-enum-members-and-refused-bodies.md) | An enum member is found through its *type*; a refused body warns, and running one fails | Accepted |
| [0048](0048-operator-overloading.md) | `operator +` is a constant whose name is an operator; one operand must be declared locally | Accepted |
| [0049](0049-for-labels-and-defer.md) | `for` iterates three known shapes; a label names a loop; `defer` runs at every scope exit | Accepted |
| [0050](0050-using.md) | `using` promotes a struct's fields into scope; a real local always wins, silently | Accepted |
| [0051](0051-aggregate-returns.md) | An aggregate is returned through a caller-allocated `sret` pointer, uniformly by size | Accepted |
| [0052](0052-multiple-return-values.md) | `-> (s64, bool)` is a structural results aggregate; `_` discards a result positionally | Accepted |
| [0053](0053-named-and-default-arguments.md) | A named argument matches a parameter name; a default must be a literal | Accepted |
| [0054](0054-scope-visibility.md) | `#scope_module` hides what follows it from importers; export is the default | Accepted |
| [0055](0055-imported-constants.md) | An imported constant`s value crosses the boundary the way a callee does | Accepted |
| [0056](0056-float-constants-are-not-integers.md) | A compile-time float result is interned as a float, not as an integer | Accepted |
| [0057](0057-implicit-context.md) | `context` is a real hidden parameter, leading rather than trailing (amends ADR-0001) | Accepted |
| [0058](0058-bounds-check-build-setting.md) | The bounds-check build setting, and `#no_abc` on a procedure (amends ADR-0003) | Accepted |
| [0059](0059-indirect-calls.md) | A procedure is a value you can call through a pointer | Accepted |
| [0060](0060-null-and-a-memory-source.md) | `null` is a context-typed pointer literal, and `malloc`/`free` reach libc | Accepted |
| [0061](0061-vm-malloc-from-its-own-region.md) | The VM satisfies `malloc`/`free` from its own region (corrects ADR-0060 §4) | Accepted |
| [0062](0062-the-allocator-protocol.md) | `context.allocator` is a struct of procedure pointers | Accepted |
| [0063](0063-push-context.md) | `push_context` gives a block its own copy of the context (amends ADR-0057 §2) | Accepted |
| [0064](0064-pointer-arithmetic.md) | Pointer offset (`p + n`, `p - n`) is element-scaled, unchecked, and lowers to an indexed address | Accepted |
| [0065](0065-temporary-storage.md) | Temporary storage is a lazily-allocated bump arena in two context fields | Accepted |
| [0066](0066-trap-backtraces.md) | A trap reports the call chain of the frames that still exist | Accepted |
| [0067](0067-switch-and-exhaustiveness.md) | `switch` with exhaustiveness from the pool, and W4.5 moves before W4 (amends §2.1's order) | Accepted |
| [0068](0068-tagged-variants.md) | `variant` is a tagged union with a checked read, destructured by `switch` (completes W4.5) | Accepted |
| [0069](0069-run-across-files-and-in-a-body.md) | A `#run` may call an imported procedure and appear in a body; W4 is split into sub-waves | Accepted |
| [0070](0070-array-length-from-a-constant.md) | An array length may name a literal-valued constant (amends ADR-0039 §3a) | Accepted |
| [0071](0071-type-values.md) | A type is a compile-time value; using one at run time is refused | Accepted |
| [0072](0072-insert.md) | `#insert` of a literal string, lowered where it is written | Accepted |
| [0073](0073-insert-computed-operand.md) | `#insert` of a computed string; the cycle broken by a narrow pre-pass, not by salsa's fixed-point recovery | Accepted |
| [0074](0074-aggregate-constants.md) | An aggregate compile-time value, interned field-wise rather than as a target-specific byte image | Accepted |
| [0075](0075-type-info.md) | `type_info` returns a `Type_Info` declared in `Basic`; a constant may hold a string (corrects ADR-0074's closing claim) | Accepted |
| [0076](0076-any.md) | `Any` is a `{type, pointer}` pair; a pointer converts to `*u8` only where a type is erased, and the read back is checked | Accepted |
| [0077](0077-type-info-id.md) | `Type_Info` gains a stable `id` (the pool id) so a type has a runtime identity for `any_as` to check (amends ADR-0075 §3) | Accepted |
| [0078](0078-type-info-per-kind.md) | `Type_Info` gains fixed-size per-kind facts (`count`, `element`); the variable-length field list stays deferred (amends ADR-0075 §3) | Accepted |
| [0079](0079-no-pointers-in-compile-time-aggregates.md) | A pointer or view in a compile-time aggregate is refused — it addressed the evaluator's memory and silently miscompiled (completes ADR-0074 §2) | Accepted |
| [0080](0080-code.md) | `#code { … }` is unquoted source that splices, and it is sugar over `#insert` — no `Code` value, declined until something can inspect a tree | Accepted |
| [0081](0081-polymorphic-parameter.md) | A single `$T` parameter, inferred from the call and instantiated structurally — sub-wave 1 delivers the surface, refusing a call (E0268) pending instantiation | Accepted |
| [0082](0082-instantiation.md) | A polymorphic call instantiates by expanding the HIR with a substituted procedure per structural key, checked and lowered per instantiation (lifts E0268) | Accepted |
| [0083](0083-multiple-type-variables.md) | A polymorphic procedure may introduce several type variables; the structural key is the tuple of all bindings (generalises ADR-0082) | Accepted |
| [0084](0084-nested-inference.md) | A type variable is inferred through a pointer or view parameter (`*$T`, `[]$T`) by a one-layer structural match | Accepted |
| [0085](0085-polymorphic-structs.md) | A polymorphic struct is a parameterised type keyed on `(decl, args)`; design of record for the sub-wave that builds it | Accepted |
| [0086](0086-polymorphic-structs-implementation.md) | Polymorphic structs as built: staged (zero-diff representation, then behaviour), a second instance-keyed field map, and every ADR-0085 §5 deferral held as a compile-time refusal | Accepted |
| [0087](0087-comptime-value-parameter.md) | A comptime-value parameter `$N: s64` — the surface (parses, lowers, body type-checks); a call is refused by design (E0271) pending the instantiation half | Accepted |
| [0088](0088-comptime-value-instantiation.md) | A comptime-value call is instantiated by evaluating the argument via the acyclic const-eval pre-pass and baking it into a clone; design of record, implementation deferred (a re-resolve gap to settle first) | Accepted |
| [0089](0089-array-length-from-comptime-param.md) | An array length may name a `$N` comptime parameter, read from the instantiation's baked value; a template's own `[N]T` gets a placeholder whose length-dependent checks are withheld | Accepted |
| [0090](0090-expand-macros.md) | `#expand` marks a macro whose body a call splices into the caller's scope; the surface ships with a by-design refusal (E0272) because an accepted-and-ignored directive is worse than a rejected one | Accepted |
| [0091](0091-expand-splice.md) | A `#expand` macro call splices the macro's body into the caller's scope — a generated prelude binds each argument once, a tail `return` assigns a result local; an early `return` (E0273) and a cross-file call (E0272) are refused | Accepted |
| [0092](0092-reflect-a-bound-type.md) | `type_info(T)` describes a bound type variable — bindings consulted first and seeded per body, withheld in a template, and an instantiation's `Type_Info` folded against its own check; unblocks `#modify` | Accepted |
| [0093](0093-modify-predicate.md) | `#modify { … }` is a compile-time predicate over an instantiation; the surface ships with a by-design refusal (E0274) because a parsed-and-ignored predicate would accept calls the author rejected | Accepted |
| [0094](0094-modify-predicate-lowering.md) | A `#modify` predicate is lowered as an ordinary synthetic procedure and cloned per instantiation with its bindings; **amends ADR-0093 §2**, whose stated blocker did not exist | Accepted |
| [0095](0095-modify-evaluation.md) | A `#modify` predicate runs at compile time in `file_mir` (the only host with the expanded tree, its MIR and the VM); a `false` refuses the instantiation with E0275 and E0274 is retired | Accepted |
| [0096](0096-bake-arguments.md) | `#bake_arguments` is a partial application producing a specialised procedure — a clone with the baked parameters dropped, reusing ADR-0088's mechanism; the surface ships with a by-design refusal (E0276) that replaces a leaked "please report it" gap message | Accepted |
| [0097](0097-bake-arguments-specialisation.md) | A `#bake_arguments` declaration produces a specialised procedure — a clone with the baked parameters dropped, substituted and remapped (ADR-0088 §3's steps, reused); a baked value must be a literal because const-eval runs after lowering. **W5 closes** | Accepted |
| [0098](0098-notes.md) | `@note` attaches metadata to a declaration for a metaprogram to read — its own node kind, since a note is data while the directives are instructions. **W6 — Metaprogram opens** | Accepted |
| [0099](0099-note-reader.md) | `has_note` / `note_value` read a declaration's notes at compile time — folded in sema with no VM, taking the declaration itself so a misspelling is an error rather than a silent `false`. §4 refuses `==` on an aggregate (E0278), a leaked ICE found by probing | Accepted |
| [0100](0100-note-query.md) | `noted_count` / `noted_name` query a file's noted declarations — folded, so both arguments must be literals; §2 states the honest limit (no `for` loop without a compiler-emitted table) and names the wave that lifts it | Accepted |
| [0101](0101-note-driven-codegen.md) | `noted_insert` generates code for every noted declaration — the metaprogram loop lives *inside the fold*, so generation needs no static-data table; §2 corrects ADR-0100 §2, which deferred generation and inspection as one thing; §3 fixes a stale `ExprId`-keyed fold, a latent miscompile a verifier panic exposed | Accepted |
| [0102](0102-build-script-output.md) | A build script names its own artefact — `BUILD_OUTPUT`, a declared constant (not an order-dependent intrinsic call) that `jr build` reads through `file_consts`; `-o` still wins | Accepted |
| [0103](0103-string-module.md) | `String` is a module of **non-allocating** byte operations — W7 opens. It exists because ADR-0099 §4 refused `==` on two strings and named a byte loop as the fix; its own module so two-module imports are finally exercised | Accepted |
| [0104](0104-sort-module.md) | `Sort` orders a view given a comparison — insertion sort for stability and no allocation. §1 and §2 fix **two leaked internal errors** in cross-file polymorphism that writing a library found: an imported procedure used as a value, and an imported template call (now E0268) | Accepted |
| [0105](0105-array-module.md) | `Array` is a **fixed-capacity** array, and three probed refusals decided that rather than effort: typed allocation is unreachable (E0232), inference through a parameterised struct is deferred, and such a struct cannot cross a module boundary (E0269) — so a polymorphic one would be unusable by every importer | Accepted |
| [0106](0106-typed-allocation.md) | `size_of` / `typed` / `untyped` make heap storage reachable **without widening `cast`** — the target type is a type argument at a searchable boundary, not an assertion buried in an expression. §2 fixes a pre-existing store-to-load forwarding miscompile only this could reach | Accepted |
| [0107](0107-growable-list.md) | `List` is a genuinely growable array. §2 fixes a **VM miscompile** — `malloc` shared the frame bump cursor, so heap memory allocated in a callee was reclaimed on return — which made the two engines disagree: the corpus differential's first real catch | Accepted |
| [0108](0108-module-diagnostics.md) | A program's diagnostics are **every reachable file's** — a root whose imported module was broken used to check clean and fail inside an engine. Each diagnostic keeps the module's own span, because attributing it to the `#import` discards the line to fix | Accepted |
| [0109](0109-view-from-pointer.md) | `view(p, n)` builds a `[]T` from a pointer and a count, so `sort_ints(elements(*l))` sorts a growable list in place — the library composes. It revisits ADR-0044 §4, whose stated reason had expired; §2 fixes a sixth leaked gap report (a view returned by value had no place) | Accepted |
| [0110](0110-null-call-traps.md) | Calling a **null procedure pointer** traps in both engines — it leaked an internal error naming an arity nobody wrote (the VM decoded null to file 0 proc 0, an arbitrary real procedure). The VM handle is biased by one so zero means null; `valid/048` proved the bias necessary | Accepted |
| [0111](0111-string-allocating-half.md) | `String`'s allocating half — `concat` / `substring` / `to_upper` / `to_lower` / `free_string` — allocates through `context.allocator` and the caller frees. Not `talloc` (a result expiring on an unrelated reset is a trap) nor an explicit parameter (the context carries it). Settles ADR-0103 §3's deferred fork | Accepted |
| [0112](0112-math-module.md) | `Math` ships the **exact closed-form** functions only — `abs`, `min`, `max`, `sign`, `clamp`, `pow`, `gcd`, `floor`/`ceil`/`round` — because a float cannot cross the FFI boundary yet, so libm is unreachable and an approximation could make the two engines disagree on the last ulp | Accepted |
| [0113](0113-random-module.md) | `Random` is a caller-owned xorshift64 generator — state a caller threads, so a sequence is reproducible in both engines. §3 records a language gap it surfaced: a `u64`-range named constant has no `name : T : value` form, so it needs `#run` of a typed procedure | Accepted |
| [0114](0114-ffi-floats.md) | A float may cross the **FFI boundary** — passed in a float register (xmm0/d0), not as a word, in both engines. libffi is told the arg/return is a float; native uses an F32/F64 AbiParam. Unblocks Math's transcendentals as a libm wrap | Accepted |
| [0115](0115-math-transcendentals.md) | `Math`'s transcendentals (`sqrt`, `sin`, `cos`, `exp`, `ln`, `powf`) are **libm wraps**, now that ADR-0114 let a float cross the FFI boundary — correctly rounded and identical in both engines because both call the same libm. Closes ADR-0112's deferred item | Accepted |
| [0116](0116-hash-map.md) | `Int_Map` is an open-addressed hash table (linear probing, tombstones, 3/4-load growth) — a heap array of structs. §2 fixes a comptime miscompile it surfaced: the wrapping operators computed in i128 and overflowed, the second engine divergence the differential caught | Accepted |
| [0117](0117-cross-file-parameterised-structs.md) | A **parameterised struct may cross a module boundary** — the *importer* resolves its fields, from the declaring file HIR, because a TypeRef indexes that file arena. Identity stays the declaring file. Unblocks generic Map/List/Array, named by three library sub-waves | Accepted |
| [0118](0118-generic-containers.md) | The containers become **generic structs with concrete procedures** — half a conversion, because inference through a parameterised struct is still deferred. `Map` stays concrete: an intrinsic type argument is not parsed in type position, a fourth small named unblocker. §4 closes two more unused-import traps | Accepted |
| [0119](0119-intrinsic-type-arguments.md) | An intrinsic may take a **parameterised type argument** — `size_of(Slot(s64, s64))`. Three separate refusals of one construct (sema, the resolver flag, MIR scan), each fix revealing the next. Unblocks `Map($K, $V)`, so all three containers are generic | Accepted |
| [0120](0120-expansion-fixed-point.md) | Expansion reaches a **fixed point** and the two expansions **compose**. Redirects come from the final check, so a template calling a template resolves; a computed `#insert` no longer disables instantiation file-wide; a clone inherits its template body's `#run`/`typed`/`any_of` values. Four programs that reported `no routine for file N proc M` while `jr check` said 0 errors. E0280 non-convergence, E0281 `$N` after a splice | Accepted |
| [0121](0121-comptime-step-budget.md) | Compile-time execution has a **step budget** — 10M instructions, `VmError::Exhausted` → E0230. A `#run while true {}` hung `jr check` outright and wedged the `jr lsp` worker on a merely-*opened* file, because salsa's cancellation cannot reach a loop that never touches the database. `Mode::Runtime` stays unmetered: there a long loop is the user's program working | Accepted |
| [0122](0122-build-output-confinement.md) | A **declared `BUILD_OUTPUT` is confined** to the working directory — no absolute path, no `..`, no leading `-`. It is computed by compile-time code in the file being compiled, and nothing checked it, so `"../../.git/hooks/pre-commit"` made `jr build` write an executable git runs on the next commit. `-o` stays unconfined (the operator, not the artefact); `jr-link` no longer trusts its caller | Accepted |
| [0123](0123-cross-crate-code-uniqueness.md) | Diagnostic-code uniqueness is enforced **across crates** by one test that reads the union — the invariant no per-crate test can state, and the one the E0200/E0201/E0202 collision violated for waves. Also checks that a constant named after a code binds it, and that `AGENTS.md`'s first-free claim is true. Two of three drifting range tables become pointers | Accepted |
| [0124](0124-two-latent-traps.md) | Two **latent** traps closed structurally, neither a live defect. Procedure attributes become an exhaustive `ProcAttr` enum, so the eighth token-set bug is a compile error rather than fourteen cascading errors. And `resolve_instance_fields_in` narrows `type_bindings` to the instance's own arguments, making ADR-0117's stated invariant true rather than held by an unrelated E0212 deferral | Accepted |
| [0125](0125-print-int-and-doc-reconciliation.md) | **`print_int` is executed** — the README's flagship capability, which nothing ran, so both engines could have broken it with all six gates green. And the documents are reconciled: `README.md` claimed Linux "is kept green in CI" when **no CI run has ever happened**, the Absent column listed three shipped features, seven "Open" items had shipped and five had a **stated reason that had expired** | Accepted |
| [0126](0126-foreign-pointer-span-bounds.md) | A foreign call's pointer span is bounded by the VM's **own** check. `marshal` validated one byte and the capture path then read the program's `count` bytes through it, so `write(1, s.data, 4_000_000)` read ~3 MB past the region and `2e9` was `SIGBUS` — while native wrote 114,688 bytes, the third engine divergence. Validated before marshalling, where the Jairs address still exists; the `unsafe` is deleted rather than corrected. §1 also records the comptime FFI gate and ADR-0107's heap fix as verified sound | Accepted |
| [0127](0127-expired-deferrals.md) | **Expired deferrals** swept out of the code, and a narrower message for `void`. A user hit "nested procedures and local constants arrive in wave **W2**" six waves after W2 shipped — and W2's row never listed them, so the note named a wave that had both passed and never owned the feature. Eleven such sites corrected to say what is *owed* rather than when it arrives. E0212 no longer claims `void` "is not a type name" while `size_of(void)` folds to 0. §3 records six features a completed wave promised and did not deliver — `[..]T`, `it`/`it_index`, `$$T`, instantiation backtraces (whose machinery exists but has **no production caller**), `Math` vec/mat/quat, and nested declarations | Accepted |
| [0128](0128-instantiation-backtraces.md) | **A diagnostic inside an instantiation names the call that demanded it** — the sharpest gap ADR-0127 §3 found, because the machinery *looked* present: `InstantiationFrame`, `with_frame` and a renderer had existed since the slice, defined early so this would not need retrofitting, and W5 shipped without ever constructing one. The actual gap was that `Instantiation` carried no span. Frames are attached by watermark rather than threaded through hundreds of `push` sites. A multi-level chain is still owed, and for ADR-0120's reason: sites are harvested from the first round, so a call inside a template's body is attributed to the template | Accepted |
| [0129](0129-enum-member-from-a-constant.md) | **An enum member's value may name a literal-valued constant**, generalising ADR-0070 rather than amending it — **not** one of ADR-0127 §3's six promises but the *generalisation* §2 recorded as owed while correcting E0237's note — the first evidence that a deferral saying what is *owed* pays for itself. One `named_constant_int` now answers for both callers and returns `i128` so each applies its own range: a length rejects a negative, a member accepts one. E0237 splits its message the way ADR-0070 §3 split E0233's, and `type-errors/028` is rewritten because its construct became legal. §4 leaves the **sibling-member** case undecided on purpose — it is a precedence fork, not an omission — and records a suppression that was implemented, measured and reverted because `Expr::Name`'s `res` is `Res::Error` even when resolution succeeded | Accepted |
| [0130](0130-math-vectors.md) | **`Math` vectors** — `Vector2/3/4` with `+ - * / ==` as operator overloads, the third of ADR-0127 §3's six promises, in the part needing no language change. The design rests on one fact that was **probed before anything was written**: an operator overload *does* cross the module boundary, so an imported `Vector3` gets real `+`. Three language limits shape the API — no procedure overloading (hence `dot3`, a gap showing through a library's names), no unary overload (hence `negate3`), and no operators inside a `$T` body (hence three concrete types rather than a generic vector). `normalize` of a zero vector answers zero, not NaN. Matrices and quaternions are sub-waves 3b/3c, because row-major-versus-column-major and matrix-times-vector are real forks | Accepted |
| [0131](0131-math-matrix4.md) | **`Math` `Matrix4`** — the fourth of ADR-0127 §3's six unkept promises. Storage is **column-major** (`values[col*4 + row]`) so the translation column is `values[12..15]`, matching GLSL and reading `A · (B · v)` right-to-left; rejected: row-major would need a per-frame transpose against every future graphics API. `operator *` covers all four meaningful pairs — matrix×matrix, matrix×vector, matrix×scalar in both orders — but **not** `Vector4 * Matrix4`, which would be row-vector times matrix and contradict §1. Projections and `mat4_look_at` are **right-handed** because `cross` already is; left-handed here would make the vector module's sign a lie one file over. `mat4_inverse` and `mat4_determinant` are deferred deliberately: the matrices this library *constructs* have closed-form inverses that are cheaper and more stable, and shipping a general inverse would make the wrong idiom convenient | Accepted |
| [0132](0132-math-quaternion.md) | **`Math` `Quaternion`** — the fifth of ADR-0127 §3's six unkept promises, closing the `Math` row. Storage is `{x, y, z, w}` to match `Vector4` in the same module, so a component-keyed transfer between the two does not need renaming; rejected: `{w, x, y, z}` is textbook order but would make `v.x = q.x` a silent bug. Multiplication **does not** auto-normalise, because the `sqrt` would be a tax on a hundred-rotation composition for a drift the caller can correct once. Zero cases answer the identity, mirroring [normalize3]'s discipline. `quat_slerp` falls back to normalised linear interpolation at `dot > 0.9995`, where slerp's `sin(θ)` denominator collapses. `quat_rotate` uses the eight-op Rodrigues form rather than the 32-op `q * v * q_conj`. Handedness is inherited from ADR-0131 §3, so `quat_to_matrix4` and `mat4_rotation_*` agree by construction | Accepted |
| [0133](0133-it-and-it-index.md) | **`it` and `it_index` in a nameless `for`** — the sixth and last of ADR-0127 §3's unkept promises to be surfaced, and the wave `for xs { it }` waited for. Ordinary injected locals rather than reserved keywords: a body may declare `it := something_else` and the shadowing works exactly as it would for any local — rejected: reserving `it` would carve out a name across every program for a construct many never use. `it_index` is injected for sequences and **absent for ranges**, because a range's counter *is* its value in MIR and adding a second counter is the same MIR fix the still-broken `for x, i: a..b` shape wants. Parser fix + HIR-lowering fix + jr-fmt fix + tree-sitter grammar update; the previous `reserved_keyword_for_is_rejected` test moved from `for x { }` (now valid) to `for;` (still an error) | Accepted |
| [0134](0134-nested-procs-and-local-consts.md) | **Nested procedures + local constants** — lifts E0207. `X :: <value>;` inside a procedure body is now legal: hoisted into `items` (so it is checked and lowered like any other item) but its name **is not** added to `hir.scope`, so two nested items with the same name in different enclosing procs do not collide. A new `Item::nested: bool` flag distinguishes them from user-visible duplicates. Nested procs recurse and see their siblings via a sibling-scope injection every hoisted body receives; **no capture** — the enclosing body's locals are not in the nested body's scope, which falls out of ordinary resolution and shows up as an ordinary E0201. Rejected: a dedicated capture-refused code (deferred until it has separately-testable failure modes); rejected: name-mangling nested items (leaks into diagnostics). The reservation trick that makes item-slot allocation come *before* body lowering keeps nested-in-nested predictions consistent | Accepted |
| [0135](0135-range-with-index.md) | **Range iteration with an index** — closes ADR-0133 §2. `for x, i: a..b` was E0227 (`x` uninitialised) and `for 0..5 { it_index }` was E0201 (`it_index` unresolved), both because MIR's range counter *was* the value and never wrote a separate index. Now: for a range, MIR keeps `value` as the counter and writes `index = value - range.start` at the top of the body — the offset-from-start definition, so `for x, i: 5..10` binds `x` to 5..9 and `i` to 0..4. `it_index` is now injected unconditionally for a nameless `for` (ADR-0133 §1 had elided it for ranges pending this fix). Rejected: two independent counters (an extra SSA phi that const-prop deletes only for `start == 0`); rejected: refuse the shape (ADR-0058 §3 has refused a "half-worked construct" seven times) | Accepted |
| [0136](0136-dynamic-array-syntax.md) | **`[..]T` — a compiler-known dynamic array** — wave 6. Retires E0124's "arrive in a later wave" refusal. Layout is `{data: *T, count: s64, capacity: s64}` — three words, matching `modules/List`'s `Int_List`. Its own `Item::DynamicArrayType`, `TypeRef::DynamicArray` and three `Projection` variants (data/count/capacity) rather than a struct alias, for the same reason `string` is separate from a struct of the same shape (ADR-0004): a user-written struct is a *different* type and never coerces. Three pseudo-fields, **all places** — `.data` is exposed here where a view's is not, because a dynamic array *owns* its heap block. Ops stay in Jairs (a library `push` on top). Rejected: two-word value + heap-side capacity (indirection tax); rejected: `Dynamic_Array($T)` polymorphic struct (import friction); rejected: native `push` intrinsic (policy belongs in a library) | Accepted |
| [0137](0137-double-dollar-t.md) | **`$$T`** — wave 7. `$T` inference *plus* `$N`-style compile-time-constant baking, in one parameter. Parser produces a `POLY_TYPE` node with two `DOLLAR` tokens (single `$` for `$T`, double for `$$T`); AST's `PolyType::is_comptime()` counts them. HIR sets `Param::comptime` for either syntactic route. Sema's `callee_poly` drops its `!comptime` restriction and `check_polymorphic_call` also records comptime arguments, so a mixed template's instantiation carries both the type bindings and the baked values. Refusals inherited: E0271 (non-constant `$$T` argument), E0268 (a `$T` no argument position pins). Rejected: a new `POLY_COMPTIME_TYPE` kind (every downstream match would grow) and a third `check_mixed_call` (duplicates `check_polymorphic_call`). One test file, `valid/110`, exercises `$$T` over `s64` and `bool` | Accepted |
| [0138](0138-variadic-declaration.md) | **`..T` variadic parameter — declaration surface** — wave 8, closing the eight-wave programme. `args: ..T` parses; HIR wraps the parameter as `[]T` so the callee sees an ordinary view. Rejected: shipping the whole feature in one wave (call-site packing is real MIR work — a stack array + a synthesized view — and the type-check side of the callee is ordinary, so refusing the declaration would refuse a program the compiler otherwise handles); rejected: a `VARIADIC_PARAM` node kind (the marker is a scalar property of an otherwise-ordinary `PARAM`, and downstream matches would grow). At the call site: an explicit `[]T` view works today; the sugar `print(fmt, a, b, c)` is refused with E0216 and a note pointing at the follow-up. `ProcSig` gains `variadic_params: Vec<bool>`; jr-fmt emits the `..` explicitly (seventh consecutive round-trip discipline for the lossy-CST trap) | Accepted |
| [0139](0139-variadic-packing.md) | **Variadic packing — the call-site sugar** — follow-up to ADR-0138, completing Wave 8. At each variadic call, MIR allocates a stack `[N]T`, stores each trailing operand into `array[i]`, takes `&array[0]` as the view's `data`, and stores `count = N` — the same shape a slice expression uses (ADR-0044 §4). Sema disambiguates the exactly-one-trailing case by natural type: a `[]T` argument is pass-through, anything else is packed. Zero, one, or many trailing arguments all record a variadic-pack entry (empty view for zero). Rejected: heap-allocate through `context.allocator` (a stack array has the right lifetime); rejected: one shared array across calls (would clobber a live view); rejected: type each trailing arg against `[]T` first and then re-check against `T` (would fire E0214 for every literal argument and duplicate diagnostics). E0214 is reused for the "expected `T` or `[]T`, found …" case | Accepted |
| [0140](0140-dynamic-array-operations.md) | **`[..]T` operations — converting `modules/List`** — the first of PLAN §7's owed follow-ups, closing ADR-0136 §4. `modules/List`'s hand-rolled `List :: struct($T)` is **deleted** and its `push`/`pop`/`get`/`set`/`clear`/`free_data`/`is_empty`/`elements`/`grow` now take `*[..]s64` — the native type's place pseudo-fields answer `.data`/`.count`/`.capacity` exactly as the struct's did, so the conversion is behaviour-preserving (`valid/088`/`089` keep exit codes 255/63). Routines stay concrete `s64`: the type crosses module boundaries but an imported *template* is still refused (E0268, ADR-0104 §5). `Type_Info_Kind.DYNAMIC_ARRAY` added to `modules/Basic` — the compiler already emitted it by name (before the member, `type_info` of a `[..]T` was a hard const-eval error), appended not inserted so no member's ordinal moves. Rejected: keep both struct and native module (one layout maintained twice); rejected: a `List(s64)` compatibility name (a same-shape struct never coerces to `[..]s64` and there is no type alias, so it would mean keeping the struct). No compiler change; `valid/113` added | Accepted |
| [0141](0141-any-variadic.md) | **A `..Any` variadic** — the second owed follow-up (PLAN §7). Probed first and found already composing: ADR-0138 makes a `..Any` callee see `[]Any`, and ADR-0139's per-argument packing checks each trailing arg through `check_arg`, which is also ADR-0076 §1's `*U`→`Any` coercion — so `f(*a, *b, *c)` erases each pointer and packs a `[]Any` with no new compiler code. One gap fixed: ADR-0139's exactly-one-trailing disambiguation compared the natural type against the element directly, bypassing the coercion, so `f(*a)` errored while `f(*a, *b)` worked. The `*U`→`Any` decision is extracted into `record_any_coercion`, shared by both paths (no re-check, no double diagnostic). MIR unchanged — a coerced argument already lowers to an `Any` before packing. Arguments are pointers; bare values (`f(42)`) stay E0214, ADR-0076 §4's deferred bare-value→`Any`. Rejected: auto-spill a bare value at the variadic slot (now cheap — packing allocates stack storage — but its own fork about implicit temporaries); a distinct `..Any` rule separate from the scalar `Any` coercion (two chances to disagree). No new code; `valid/114` added | Accepted |
| [0142](0142-optimisation-level.md) | **An optimisation level** — opens W8, closing ADR-0058 §6's deferral. `--opt-level 0|1` (short `-O`) on `jr run` and `jr build`, defaulting to 1 = today's behaviour; `-O0` skips `jr_mir::optimize` entirely, so the back end receives exactly what `file_mir` lowered (asserted byte-identical, which is what makes `-O0` usable to attribute a miscompile to lowering rather than to a pass). `OptLevel` is a second `BuildConfig` field — a salsa input for ADR-0058 §2's reason — and an enum rather than a `u8` so the query's match is exhaustive. Two levels because there are two behaviours: a `-O2` running the same four passes would be a flag whose only content is a promise. Rejected: `--release` (a bundle, and ADR-0058 unbundled bounds checks from speed on purpose), a bare `-O` boolean, and a per-command default (`jr run` at `-O0` would make the corpus differential silently cross-level). The wave's real deliverable is the check the mid-end has never had: every corpus program's whole observable behaviour is identical at both levels, swept in the VM because the mid-end is upstream of both back ends. The one legitimate difference — an inlined leaf's trap names the call site at `-O1` and its own line at `-O0` (ADR-0021 §3) — gets its own named test rather than being excluded from the comparison, which gives `-O0` a second use: an honest backtrace | Accepted |
| [0143](0143-llvm-backend.md) | **The LLVM back end, and a third engine** — W8 sub-wave 2, closing ADR-0019 §5's assignment of `jr-codegen-llvm` to this wave. `inkwell` behind a default-off `llvm` feature plus a **seventh gate**, because `llvm-sys` needs an LLVM 21 it can find and homebrew's `llvm@21` is keg-only — an unconditional dependency would wall off the whole compiler for a non-default back end. `--backend cranelift\|llvm` on `jr build`; the flag exists without the feature and the driver refuses with a message naming it, so "unknown argument" is never the diagnostic for a missing capability. Three genuine differences from Cranelift, each forced by LLVM: a block parameter becomes a **`phi`** filled from the predecessor side (MIR forbids critical edges, so this is bookkeeping rather than an unphi pass — rejected: copies in predecessors, and `alloca` + `mem2reg`, which throws away the SSA ADR-0017 built); every address is an opaque `ptr` and **every offset a byte GEP**, with *no Jairs aggregate acquiring an LLVM `StructType`*, because that would put LLVM's padding rules in charge of where a field sits — a second layout computation ADR-0018 §2 forbids; and poison must be *avoided*, so overflow uses `llvm.{s,u}{add,sub,mul}.with.overflow`, shifts and divisions are checked first, and float→int uses `llvm.fpto{s,u}i.sat` to match ADR-0040 §4's clamping. Every `alloca` lives in a leading block that falls through, because an LLVM `alloca` in a loop grows the stack where a Cranelift stack slot does not. Two trait gaps found by using it and fixed: `Backend::libraries` (the link line was an inherent method, so a driver could only ever drive one back end) and `TrapKind`/`TRAP_HELPER` **moved to `jr-codegen`**, since the words a trapping program prints are shared vocabulary the differential compares byte for byte. All 114 corpus programs and every hand-tried trap agreed with the VM on the first run — prospective value, not a defect found. Not done: LLVM optimisation passes (`-O` still selects only the mid-end), debug info, cross-compilation | Accepted |
| [0144](0144-align-and-place.md) | **`#align` and `#place`** — W8 sub-wave 3, §2.1's layout control. Field attributes after the type — `x: s64 #align 16;`, `y: s64 #place 32;` — each with its own `SyntaxKind`, looped so the order is the author's. Operand is an integer literal *or* a literal-valued constant, through `named_constant_int`'s **third** caller (ADR-0070 wrote it, ADR-0129 generalised it, and it needed no change to serve one); arithmetic stays refused for ADR-0018 §3's ordering reason, the same limit an array length has. **Two decisions were found by building rather than planning.** `#align` is a **minimum** (`max(natural, N)`): the draft refused a lowering value, and the compiler cannot make that check — a field's natural alignment needs `layout_of`, and during signature resolution a field whose type is a struct resolved later has no layout, so the rule would be enforced *sometimes*. Reading it as a minimum removes the problem instead of hedging it, and matches `#[repr(align(N))]`. And a **misaligned `#place` is allowed**, because probing found the LLVM back end already claiming `load … align 8` on addresses it computes itself and has proved nothing about — undefined behaviour that predates `#place`; it now claims `align 1` everywhere but an `alloca`, which is sound for every field and free at `OptimizationLevel::None`. Overlap is unchecked and is the point (what a `union` cannot say when only *some* fields overlap); a placed field never moves the ones after it, since the cursor takes the maximum end so far. Rejected: Jai's cursor-based `#place <name>;` (needs name resolution before layout, and makes one field's declaration change another's offset), a struct-level `#align`, `#align` on a local or procedure, and any packing/lowering form. **No engine changed for the feature** — it is `jr-pool`'s fold plus the syntax, which is ADR-0018 §2's claim tested by a layout *feature* rather than a layout *fix*. New codes E0282/E0283 (`jr-sema`) and E0132 (`jr-syntax`); `valid/115` exits 114, a checksum of offsets | Accepted |
| [0145](0145-inliner-maturity.md) | **Inliner maturity** — W8 sub-wave 4, closing half of §1.5's "cross-block forwarding and SROA" and taking up ADR-0021 §4's deferred cost model. The **leaf rule is gone**: a callee containing a call is now inlinable, which is the `sort_ints` → `sort` → `less_int` shape a standard library is full of and the leaf rule refused entirely. **A recursive callee is refused instead, and the reason is backtraces, not termination.** The draft unrolled recursion three levels — correct, and it broke `a_recursive_trap_reports_every_live_frame_in_both_engines`: an inlined callee has no frame (ADR-0021 §3), ADR-0066 §4 defers inline-provenance backtraces, and in a recursive trap the *depth* is the message, so flattening three of four frames is a backtrace that lies. The check walks the bodies available for inlining (a cycle whose members are not all available cannot be spliced through anyway) and catches mutual recursion too — deliberately by the same check, since a cheap self-call test would have flattened `ping`/`pong` while reporting the direct case correctly. Termination then has two guards for two jobs: the cycle check (structural, for cycles) and `MAX_INLINE_ROUNDS = 3` (bounds nesting *depth*, since a splice copies the callee's calls in and visiting them only next round makes the round number the depth — no per-statement provenance MIR does not carry). Plus `MAX_INLINED_STATEMENTS = 256` against a fan-out the leaf rule used to make unlikely. **Forwarding now follows a single-predecessor chain** up to 8 blocks: sound because one predecessor means it ran first *and* dominates the load, which is what makes a `ValueId` usable there; a join ends the chain, so a store before a loop still is not forwarded. Rejected: an SCC analysis, an unphi-style copy pass, a full available-expressions dataflow (a bigger change that would land beside this one and make a divergence ambiguous), and chaining through a join "when all predecessors agree" (that *is* the meet, written informally). **SROA stays deferred with a sharper reason**: it needs a new `Rvalue` extracting a field from an operand — a MIR change reaching three engines — and this wave's own output makes the case, since inlining `print` leaves exactly that pattern three times in `024-hello`'s optimized MIR. `valid/116` exits 112 | Accepted |
| [0146](0146-compile-throughput.md) | **A compile-throughput number, and the faster sort it justifies** — W8 sub-wave 5, taking up §2.1's "published compile-throughput number" and ADR-0104 §3's "a faster algorithm is W8's, with a benchmark behind it". `jr bench --throughput <PATH>…` is a **mode** of the existing subcommand rather than a new one, because it is the same contract (measure, report, never judge, ADR-0033 §4) and a second subcommand would be a second place to add a threshold. Times `check` (every diagnostic for every reachable file) and `build` (through MIR into an object, excluding the link, which is `cc`), **cold only** — a compiler is a process, so a warm figure would be measuring a memo table. Reports lines/s *and* bytes/s, because a line is a formatting artefact in a corpus whose lines are largely `//!` prose. Published with the machine, the toolchain and the input set, and dated: a stale number that says when it was taken is usable, one that does not is a lie. **116 files / 9 203 lines: check 113 k lines/s, build 26 k lines/s** on an M2 Pro release build — and `build` is 4.4× `check`, which says the front end is not where the time goes. `modules/Sort` gains **`heap_sort`**: in place, no allocation, `O(n log n)` *always*, and **unstable** — so it sits beside `sort` rather than replacing it, because stability is observable behaviour and swapping it silently would change what an existing program computes (the Rust `sort`/`sort_unstable` split). Rejected: a hybrid (stability would depend on input *size*), a merge sort (reopens ADR-0103 §3's allocation decision), quicksort (`O(n²)` worst case plus a pivot argument). The choice is proved by a **comparison count**, not a timing: deterministic, machine-independent, identical in all three engines, so `valid/117` asserting heapsort makes strictly fewer comparisons on a reversed input is a test in the differential harness. Two findings recorded rather than fixed: a `$T` template cannot call another `$T` template (E0268 — heapsort is one loop with a single sift site as a result), and a file-level mutable variable leaks an internal error, the eighth of that shape | Accepted |
| [0147](0147-soa.md) | **`#soa` — a struct stored as arrays** — W8 sub-wave 6, §2.1's last *language* item for this wave. `Entities :: struct #soa(4) { x: s64; hp: u8; }` lays out as one array per field, and **`e[i].x` means `e.x[i]`**. The access sugar is not separable and the staging was refused for that reason: without it `#soa` buys almost nothing over writing `[N]T` by hand, since the count can already be a named constant. The transformation happens in `jr-sema` *before* layout — each field's resolved type is wrapped in `[N]T` — so layout, field offsets, `type_info`, the VM and both back ends saw an ordinary struct of arrays and needed no change; rejected: an `Item::SoaType`, which would make every match on `Item` in five crates grow to say "the same as a struct". The access is a *place-order* rewrite: sema records the field position and `jr-mir` builds `Field(n)` then `Index(i)`, because two crates recognising the pattern independently is the "two searches for the same thing" trap and a disagreement here is a wrong **address** — sema typing an element while lowering reads a whole array. A bare `e[i]` is E0284: it has no type by design, and Jai's synthesised struct-of-pointers is a real but much larger design that refusing leaves available. `using` inside an `#soa` struct is refused too (each promoted name would be an array, with no spelling for the index). Three things building it found: the `#soa` predicate belongs in the **pool**, because sema asks it about types declared in other files (ADR-0117); an expression id indexes a *body*'s arena, so the first hooks read the wrong one and the sugar silently did nothing inside a procedure; and `e[i]` needs a *recorded* type even though it has none, because `scan` refuses a body holding an `ERROR`-typed reachable expression — it takes the receiver's. `valid/118` exits 126 and asserts the two spellings agree | Accepted |
| [0148](0148-simd.md) | **`#simd` — a vector type, at the width the hardware actually has** — W8 sub-wave 7, §2.1's "SIMD vectors" and PLAN §7's *types or layout attributes* fork. `v: #simd [4]s32` is one machine register; `a +% b` adds four lanes at once. **Probing settled the design twice before any of it was written.** Cranelift compiles a vector op at exactly 128 bits and nowhere else — `iadd` on an `I64X4` fails with `Unexpected SSA-value type`, though the *constructor* happily makes one — so the legal set is the six shapes a register holds, and that machine fact is deliberately in the language (E0285 names the six rather than stating the rule); rejected: any N with the back end splitting (vector legalisation is a mid-end job, ADR-0017 §1, and LLVM would split while Cranelift could not, so the differential would compare two different programs) and any N lowered elementwise (a type with no machine meaning and, per ADR-0146 §4, no way to measure whether it helped). And **integer division is not a vector operation** — the verifier refuses `sdiv` at every lane count, because no ISA has one — refused rather than scalarised, since a silent pessimisation in a construct chosen for speed is the performance twin of a silent miscompile. The fork's answer: the *syntax* is an attribute (layout is the array's) and the *type* is its own `Item` (three questions differ — representation, operators, whether the count is chosen), which is the opposite call to ADR-0147 §1's refused `Item::SoaType` and consistent with it for a stateable reason: a new `Item` earns its five matches exactly when the arms differ. **§6, found while building: an integer vector takes `+% -% *%` and refuses `+ - *`** — no vector add can trap, so letting `+` wrap would give one spelling two meanings and making it trap needs a compare and branch per lane; ADR-0002's opt-out already existed for this. The VM loops where native emits one instruction, the first operator whose engines execute a different *number* of operations. `valid/119` exits 170 in all three, and `S32_MAX +% 1` is the lane that carries the claim | Accepted |
| [0149](0149-parallel-sema-measured.md) | **Parallel sema, measured and refused — and the pool becomes an `RwLock`** — W8 sub-wave 8, **closing W8**. §2.1's "parallel Sema + parallel codegen" was written, worked, produced byte-identical output at 1/2/4/8/12 threads — and was then measured and **reverted**. In-process it gives 1.39x, saturating at four threads; at the process level it gives **1.20x** on the clean corpus and **1.01x** on a mixed tree, because the parallelised phase is itself a fraction of the command (reading 194 files, the one-shot `source_map()` clone and rendering every diagnostic are all serial, and the process costs 10 ms to start). Instrumenting the guard found the binding constraint: **571 acquisitions holding the pool for ~30 ms of a 74 ms check — 40% serial, so Amdahl caps any driver-level parallelism at 2.5x.** Not shipped because 1.2x buys a latent failure mode that appears *only* under threads — a `std::sync::RwLock` is neither reentrant nor upgradable, so a future query holding a guard across a nested call hangs, and `run.rs` already carries a comment about the time that happened and "the program hung rather than failing, which is worse". A `--threads` flag defaulting to 1 would be dead code; defaulting to auto is a tax. Parallel *codegen* gets a sharper verdict: the probe that appeared to measure it (119 roots concurrently) was measuring **duplicated work**, since `build_object` compiles a whole program per root — and no program here has more than four files, so this project cannot measure it at all. The one change that lands is `Mutex<Pool>` → `RwLock<Pool>`, which **made nothing faster** and is kept because it turned eight hand-rolled `pool().lock().unwrap_or_else(…)` sites in `jr-lsp` into compile errors, now one `Db::read_pool` — a duplication `run.rs`'s docs already warned about. Rust found all six read-only sites for free: they were already `let pool`, not `let mut pool`. Two named blockers for any future attempt, neither a driver change: finer-grained interning (ADR-0015/0018 §2 territory) and an input large enough that a benchmark can tell the change from noise | Accepted |
| [0150](0150-foreign-aggregate-refusal.md) | **A `#foreign` signature that cannot be lowered is refused, not crashed** — PLAN §8.6 step 1, and the cheapest item in it. Calling a `#foreign` procedure with a struct by value gave `procedure 0 in file 0 was defined without being declared` from Cranelift and `no routine for file 0 proc 0` from the VM — two different internal errors, one legal-looking program, no diagnostic: the **ninth** occurrence of this project's most-recorded failure shape, found by probing while writing PLAN §8. **The refusal already existed** — `jr-codegen-llvm`'s signature builder says "an aggregate passed across a `#foreign` boundary" in words, at a layer with no span and no user — so this wave moves a rule rather than inventing one. Now E0286 at the **declaration**, because the signature is what cannot be lowered: a binding that could never be called successfully is itself the error, and a library binding is normally written before its first caller. `foreign_boundary_refusal` matches the pool **exhaustively**, which is load-bearing rather than stylistic: `#simd` added a type two waves ago, and a `_ => None` arm would have made a vector quietly passable and produced exactly the silent reinterpretation this refuses. The message differs by *why* — a `string` is told to pass `.data` and `.count`, a struct is told the platform ABI's classification rules are missing, a return type is told to take an out-parameter, and a vector is refused with the honest reason ("one register, but no engine declares one across a C boundary") rather than a false one about width. Rejected: refusing in `jr-mir`, whose channel is `give_up` → E0245, only a *warning*, so the program would still link and still crash; and fixing it per back end, which is two copies of one rule. Every existing `#foreign` declaration in `modules/` and the corpus was checked first — all scalars, pointers and floats — so nothing that works today is broken. Converts W10's gate (PLAN §8.1.2) from a crash into a stated limitation | Accepted |
| [0151](0151-must-and-the-error-model.md) | **`#must` — the error-handling marker ADR-0008 chose and never built** — PLAN §8.6 step 2, and it unblocks five of W7's remaining modules. ADR-0008 decided this language's error model in the vertical slice — multiple returns plus `#must` — and scheduled the marker "into wave W2". Multiple returns shipped; `#must` did not, then or in the twelve waves after, so the model was half a model: a fallible operation could return a success flag and nothing could stop a caller ignoring it. **The obligation lives in the procedure's *type***, in ADR-0008's reserved `EffectRow` — inert since the slice, and this is the first thing put into it, because "the result must be received" is an obligation on a *call*, which is what an effect is. That choice is the wave: it crosses module boundaries for free (a call site has its callee's type even with no HIR for the declaration), survives being taken as a value (`f := only_one; f(10)` still checks), and cannot be dropped by assignment, since two procedure types differing only in `#must` are different types. Rejected: a `ProcId`-keyed side table, which needs cross-file threading for one bit and answers nothing for a procedure pointer; and growing `EffectRow` into a real named-effect row, since one example is not a pattern and ADR-0008's door stays open *because* `must` is a field rather than the whole struct. **Checked at exactly one place** — the `Stmt::Expr` arm, the only position where a value is produced and dropped — so nothing has to be remembered at every expression position. **`_ = f();` is the escape hatch, deliberately**: an unbypassable check is one people route around with a one-line wrapper, which hides the decision instead of recording it, and the point is visibility rather than prohibition — the same trade `#no_abc` makes. It needed `Stmt::Discard` as its own variant, because a single `_` is *not* a one-element target list: `destructured_results` refuses a single position (E0251) since a destructuring statement over a single-return call is a real mistake, so reusing the tuple would have weakened a live check. E0288 refuses `#must` on a `void` procedure at the declaration (ADR-0058 §3's rule). The formatter dropped `#must` on the first attempt — the tenth wave running for that trap, and the *unsound* direction, since losing it deletes a check | Accepted |
| [0152](0152-static-data.md) | **Compiler-emitted static data, and the field list ADR-0078 deferred** — PLAN §8.6 step 3 and §8.2's wave-sized W6 decision, which discharges *two* owed things with one mechanism exactly as §8.2 predicted. `Item::StaticArray` interns a table's element values and materialises as a `[]T` view; each engine emits the bytes once, keyed on the table's `PoolId` since the pool already deduplicated by contents. **Why it was hard is the same defect twice**: ADR-0074 found a pointer inside an interned aggregate interned the *evaluator's own address* — 48 in one engine, a segfault in the other, no diagnostic — so an address can never be a pool value. The precedent was already in the tree: a `string` literal interns a `StrId` and each engine builds the pair at materialisation, so this is that solution one type wider. **The byte image is computed once, in `jr-pool`**, because an image is offsets plus widths — the computation ADR-0018 §2 centralised so three engines cannot disagree, and three walks would have been three differently-shaped tables that no verifier could catch, each engine internally consistent and one reading the wrong field. The engine supplies only its own addresses, and the callback is told *where*, because a native engine cannot answer with a number at all — there is no address until the linker runs, so it records a relocation. **That was got wrong first and the corpus file caught it**: both native engines recorded patches and never applied them, so reading a field's name gave **139 where the VM gave 121**. Cranelift uses `write_data_addr`; LLVM has no such API on a byte initialiser, so its global is a *packed struct of chunks* with each pointer a `ptrtoint` constant expression — the layout still the pool's, only the expression of an unknowable address differing. `Type_Info.fields: []Type_Info_Field` carries name, type id and offset, the offsets from the same `field_offset` fold codegen reads, so a reflected offset cannot disagree with a compiled one. `valid/121` exits 150 in all three engines | Accepted |
| [0153](0153-message-loop.md) | **The compiler message loop — a metaprogram that iterates rather than unrolls** — W6's headline claim and PLAN §8.2's remaining item, built directly on ADR-0152 exactly as §8.2 predicted. `noted_count`/`noted_name` let a script *unroll* to a bound written into the script; ADR-0100 §2 recorded that not as a spelling limit but as the boundary of folding itself — a fold is answered while checking and a `for` variable does not exist then — and named the mechanism it was waiting for. So this is the collection of a debt, which is why it is small: `noted_declarations("x")` folds to a `[]Declaration` over a compiler-emitted table in declaration order, and the loop's bound is `routes.count` rather than a literal, so adding a fourth `@route` changes the program without editing the loop. `Declaration { name, note_value }`, with `""` for a bare note — a real answer rather than an absent marker, so no second field is needed. **Rejected: a genuine poll** (`compiler_wait_for_message()`, Jai's shape), because it implies the metaprogram runs concurrently with compilation — an execution model this compiler lacks and PLAN §8.3 has since split into W11 — and because a poll's behaviour would depend on compilation *order*, which salsa's re-execution makes unstable: the same program would answer differently between runs depending on what was memoised. A table is a value, and a value is reproducible. **Inspection and generation stay separate**: `noted_insert` generates code at compile time, this inspects at run time, and a single mechanism doing both would have to answer when a declaration added by one iteration becomes visible to the next — ADR-0120's expansion fixed point, in a place with no bound. That is also why the table carries no procedure pointer. Third client of the library-shape contract, which is where that mechanism stops being a one-off. `valid/122` exits 21 in all three engines, and its load-bearing term is that `@deprecated` is *absent* from the `@route` table | Accepted |
| [0154](0154-w6-close.md) | **W6 closes — a second build option, and two items declined with their reason** — the last of PLAN §8.2's four items. `BUILD_OPT_LEVEL :: 0;` joins `BUILD_OUTPUT`, making "a build script replaces the makefile" true of the two things every makefile does, with `-O` outranking it (ADR-0102 §2's asymmetry: a declared name is the *artefact's* choice, a flag the *operator's* instruction). **It needed a bootstrap rule `BUILD_OUTPUT` did not**: reading a declared constant means *compiling*, so an option that affects compilation cannot be read without already having chosen one — and reading it under a fixed level is sound *because of ADR-0142's check*, which asserts every program behaves identically at both levels. That check was built two waves earlier for a different reason and is what makes this legal. A `Build_Options` **struct** stays deferred but now on a *named* blocker rather than a threshold: this language has no struct literals (E0117), so `Build_Options.{ … }` does not parse. **Plugin hooks and Jai-style workspaces are DECLINED, not deferred**: both are the poll model — a hook is a callback the compiler invokes at a phase, a workspace is a compilation you poll — and ADR-0153 §1 rejected the poll because it needs an execution model this compiler lacks (now W11) and because its behaviour would depend on compilation *order*, which salsa's re-execution makes unstable. So there is nothing to hook into, and what a plugin would want exists in two reproducible pieces: `noted_insert` generates, `noted_declarations` inspects. The file-set half of a workspace exists as `reachable_files`. If revisited, the right question is a compilation unit that is a **value**, not a poll. One integration test, asserted through the *backtrace* — the one observable difference between the levels — because a test reading a driver message would pass whether or not the level reached the mid-end | Accepted |
| [0155](0155-time-buckets-stable-sort.md) | **`Time`, a bucket array, a stable sort — and four polymorphism defects the sort found** — PLAN §8.3 steps 1-3 of W7's remaining modules. `Time` is nanoseconds as an `s64` with two clocks and deliberately no formatting (a calendar needs decisions this project has not made); rejected a `timespec`-shaped `Duration` struct and a coarser base unit. `Bucket_Array` keeps element **addresses** stable by appending fixed buckets to a movable spine — the promise `List` cannot make, since it copies on growth; no removal, because compacting breaks the promise and a tombstone stops `get` being pointer arithmetic. `stable_sort` takes its scratch from the **arena** (ADR-0065's first real customer), falls back to insertion sort when it has no room, and merges bottom-up; rejected `malloc` per call, a caller-supplied buffer (written, then removed), and an in-place merge. The sort would not compile, and four separate instantiation defects came out: `typed(T, …)` refused a bound variable while `size_of(T)` beside it accepted one; an instantiation's pointer views were never threaded into MIR; E0268 refused a template calling a template; and `check_polymorphic_call` **deleted** a shadowed type binding instead of restoring it (PLAN had recorded that one). `valid/126` isolates three of the four. `Sort` also gained its first `#import` — `talloc` was unresolved, and showed up only as E0245 on the whole body | Accepted |
| [0156](0156-json.md) | **`JSON` — a flat node store, and the first module that needs both allocators** — PLAN §8.3 item 4, unblocked by `#must`. A value is an **index** into one `[..]Json_Node`, with children as a `first`/`next` chain: freeing is one call where a pointer tree needs a recursive walk that must be right on the error path too, and a handle is copyable with no ownership question. Rejected the textbook `*Json` DOM, and rejected a `Map` per object (it cannot key on a string, and a chain preserves **source order**). The **extent** of a number is found by JSON's grammar and its **value** by `strtod` (reachable since ADR-0114) — `strtod` alone accepts `0x1p3`, `inf` and a leading `+`; integers are converted in Jairs because `float64` loses 2^53 + 1. Accessors answer questions rather than assert facts, because a caller inspecting untrusted input is asking. Deliberately **no serialisation**: it needs a correct `dtoa`, and an approximate one would emit numbers the module could not read back. `String.adopt` exported for the one caller that genuinely builds a string from bytes, allocating through the **context** so `free_string` pairs — a `malloc`/context-free mismatch was written and caught, exposing the library's allocator seam. Found a MIR gap: a field of a call's *result* does not lower | Accepted |
| [0157](0157-file-and-paths.md) | **`File` and `File_Utilities` — and two silent divergences the filesystem found** — PLAN §8.3 item 5, the last modules the error model was holding up. Descriptors in a one-field struct rather than buffered streams (there are no destructors, so an unflushed buffer is data lost on scope exit); paths as `string` rather than a `Path` type that would suggest a validity check it does not perform. Everything is `#must` except `close`, which has no recovery available — but `write_entire_file` *does* check it, because its promise is different. **Two silent defects, neither in the modules.** A fixed-arity `#foreign` declaration of a **variadic** C function passes the extra argument in the wrong place: `open(path, flags, mode)` created a file with permissions `---------x` on arm64 with no diagnostic, so creation routes through the genuinely fixed-arity `creat`. And freeing a string **literal** aborts natively while running clean under `jr run`, because the VM drops a pointer it does not recognise (ADR-0061) — found by writing `out := "";` in an accumulate loop, a shape any such routine has. `String` gains `borrow` beside `adopt`: one construction, two obligations, so a call site says which. `size` uses two seeks because `struct stat` cannot cross the FFI boundary; no `readdir`, no metadata, no `errno`, each with its blocker named | Accepted |
| [0158](0158-process-and-socket.md) | **`Process` and `Socket` — W7 closes, and the VM's pointer boundary is drawn** — PLAN §8.3 items 6 and 7, so **W7 — Stdlib is DONE** (nine of nine). `execvp` is bound because it is the only `exec` that is fixed-arity, after ADR-0157 §2 showed what a fixed-arity declaration of a variadic function does; the exit status is a struct because `waitpid`'s bits are decoded by *macros* a binding cannot reach, and because `exit(1)` produces 256. **The VM cannot pass a pointer to memory that itself contains pointers**: its region-relative addresses are translated one level deep, which is all a type can support, so `execvp`'s array-of-pointers works natively and fails under `jr run`. Refusing such a call was rejected — it would also refuse `strtod`'s `char **end`, which works, and no type tells a written-to out-parameter from a read-from array. So `Process`'s test is a native `jr-cli` test, the conclusion ADR-0126 reached for its case; `Socket` is unaffected because a `sockaddr_in` holds only integers. `Pool::view_of` now interns `*elem`, closing a leaked internal error a struct-element view exposed. `parse_ipv4` is hand-written so the **refusals** are ours — `inet_aton` accepts `"1.2.3"` | Accepted |
| [0159](0159-semantic-tokens.md) | **Semantic tokens — and W9's DWARF item was mis-estimated, with evidence** — PLAN §8.4. The fourteenth and last LSP request method, and the only capability whose whole value is information the parser does not have: a grammar sees `IDENT` where a reader sees a parameter, a field, a type or a module. Classifies by **CST context first** and resolves only a bare `NAME_EXPR`, so a file that does not parse still colours — the state an editor is in most of the time. Sixteen types and two modifiers, where `readonly` is exactly what `::` means; punctuation deliberately unclassified. Full document only: a range and a delta are optimisations needing per-document state the server does not keep. The delta encoding is guarded by sorting before encoding and by taking each length from two positions, because one out-of-order token corrupts every later position and a byte length overruns under UTF-16. **And §8.4's DWARF row was false** — "line tables exist" against an empty `.debug_line`, no `__DWARF` segment and no `gimli` consumer, probed; the README's table was right all along. The item is a from-scratch writer and becomes **W12 — Debug info**, named the way §8.3 named W11 | Accepted |
| [0160](0160-c-abi-classification.md) | **The C ABI classification for an aggregate — the decision, made once** — PLAN §8.1.2 part 1 of 2, the project's highest-leverage open item: it gates W10 entirely plus `readdir`/`stat` and `getaddrinfo`. `jr_pool::classify` answers where an aggregate's pieces go, **once**, because three engines cross this boundary and a struct in the wrong register is a silent wrong answer with no diagnostic — ADR-0020 §2's argument about trap messages, with more force. Two shapes are supported: at most two words in general registers, and a homogeneous float aggregate of at most four members in FP registers. **The HFA has no size limit and that is the point** — a `CGRect` is four doubles and thirty-two bytes, and a size test would send it to memory and break every graphics call. `Memory` is a **refusal**, not an indirect pass, because the case also covers a small *mixed* struct where System V classifies each eightbyte independently and AAPCS64 does not: one case with two correct answers must be refused until it is split. Rejected implementing System V's rules against a target this project has never run. Part 2 wires the three engines and is specified in §6; landing part 1 alone changes no behaviour, so no engine can diverge while they are done one at a time | Accepted |
| [0161](0161-foreign-aggregate-wiring.md) | **An aggregate crosses a `#foreign` boundary — part 2, the wiring** — PLAN §8.1.2 complete, so **W10's gate is open** and `readdir`/`stat`/`getaddrinfo` become expressible. The VM **describes** the struct faithfully and lets libffi place it — it consults `classify` only to bound its return buffer, and would keep working if the refusal were relaxed; the `#[repr(C, align(16))]` on that buffer is load-bearing, since libffi stores a returned struct from registers. Cranelift emits `AbiParam`s per register and moves whole words from the layout's start, not per-field loads. LLVM emits **separate scalars rather than `byval`**, matching Cranelift exactly so the differential harness compares like with like. E0286 now asks the same `classify` the engines act on, so the diagnostic and the capability cannot drift — which is why part 1 shipped separately. **Verified against a C compiler, not itself**: `valid/130` calls libc's `ldiv` in all three engines and checks quotient and remainder *separately* so a register swap shows; a `cc`-compiled shim covers an aggregate argument and a nested four-`double` HFA. The Cranelift verifier caught an early `return` that emitted the results and dropped every parameter | Accepted |
| [0162](0162-c-variadic-marker.md) | **`#c_variadic` — a marker, and a refusal instead of a silent miscompile** — the first half of ADR-0157 §2's two, and W10's other gate. A fixed-arity declaration of a variadic C function puts the extra argument in the wrong place *silently* (permissions `---------x`, measured), and nothing can infer variadicity — a Jairs signature cannot say the C one ended in `...`. So it is a marker whose **absence means "not variadic"**, which is the safe default, and whose presence makes a **call** E0289. Declaring is legal on purpose: a library author can annotate `printf` today and get a diagnostic instead of corruption. Refused in **all three** engines rather than only Cranelift, even though libffi and LLVM could do it — `jr build` failing where `jr run` works breaks the premise the differential harness rests on. Rejected: inferring from the symbol name (a guess that looks like knowledge), and emitting the stack arguments by hand (Cranelift owns that layout). The formatter **deleted the attribute** on the first attempt — eleventh consecutive wave, and the most unsound direction yet, since dropping it restores the miscompile | Accepted |
| [0163](0163-w10-foundation.md) | **W10's real path — SDL2 over Cocoa, and a library search path** — §8.5 planned "Cocoa via `#foreign`", and ADR-0162 established that `objc_msgSend` cannot be called, with the blocker **upstream** in Cranelift. That removes an option rather than delaying the wave, so W10 is built on **SDL2's C API** — proven, not proposed: a Jairs program opens a window, creates a renderer, sets a colour, clears, fills a rect through a `*SDL_Rect`, presents and tears down, six for six. No `objc_msgSend`, no aggregate by value, and it works on Linux, which §0's decision #6 makes a target and Cocoa never was. The cost is stated: a third-party dependency where §2.1 imagined system frameworks. Rejected: an Objective-C shim compiled during a build (a decision about what "stdlib in Jairs" means), and hand-rolling messages on the `objc_` runtime, which needs a `#foreign` procedure *value* — E0256. The probe failed once, exactly: `ld: library 'SDL2' not found`, because a `#system_library` names *what* and never *where*. `-L`/`--library-path` and `JR_LIBRARY_PATH` now exist, `-L`s before `-l`s as `ld` requires, and **not** as a source directive — a path belongs to the machine compiling | Accepted |
| [0164](0164-window-module.md) | **`Window` — a window and a 2D surface, and the union that closes it** — PLAN §8.5 items 1 and 2, the first W10 wave, turning ADR-0163's six-call probe into a module of ten steps. Opaque C handles are named one-field structs (`File`'s reason: a handle and a count are both machine words); `Rect` is four `s32`s because SDL reads sixteen bytes and a wider field puts the *next* rect's data in this one's height, with a narrowing constructor since that is where a caller forgets a cast; every failable routine is `#must` and `present`/`stop` deliberately are **not**, because `SDL_RenderPresent` returns `void` and inventing a `bool` lies about what the library says; `delay` lives here rather than in `Time`, whose no-sleep invariant (ADR-0155 §1) is about the comptime VM and this module cannot run there at all. **The finding: no event loop, because `SDL_Event` is a union** — E0286, and ADR-0160 §3's reason is unarguable. So the boundary recurs a fourth time (`stat`, `sockaddr`, structs, now a union), and settling it — C shims or `#place` overlays — settles ADR-0163's deferred Objective-C question too. Rejected: hard-coding `event.type` at offset 0, four lines and a silent break on any point release. Native-only test that skips when SDL2 is absent, narrowly | Accepted |
| [0165](0165-event-loop.md) | **The event loop ADR-0164 said was impossible — amending ADR-0164 §5** — that section claimed no event loop was possible because `SDL_Event` is a union and E0286 refuses one at a `#foreign` boundary. The refusal is right and **irrelevant**: E0286 refuses an aggregate crossing *by value*, and `SDL_PollEvent` takes a **pointer** — the same shape as the `*Rect` that module had been passing all along. `AGENTS.md`'s habit (write the thing before planning around it) would have caught it; the correction cost one probe, four assertions, and **no compiler change**. Seventh time the habit has paid, and the first time against this project's own accepted ADR from the same session. `Event` is a `#place` overlay (ADR-0144), which is exactly the union mechanism — `key_sym` and `mouse_x` genuinely share offset 20, and the test *asserts* the sharing. Offset 0 is contractual (SDL's header: "shared with all events"); the rest are SDL2 ABI offsets that `layout_is_sdl2()` size-checks. Fields are widened to `s64`, never constants narrowed, because widening cannot be wrong — **a typed constant does not parse and is now owed**. `wants_to_close` drains, because SDL does not promise one-push-one-poll: a one-poll test passed on the first push and failed on the second. A synthetic `KEY_DOWN` is pushed successfully and then dropped by SDL, so the key assertions read a locally-built event. **ADR-0164's "four waves at one boundary" reading is withdrawn, and the Objective-C question is untouched** — it was never the same fork | Accepted |
| [0166](0166-ui-module.md) | **`UI` — immediate-mode widgets, and the sentinel that answered `true`** — PLAN §8.5's UI item, unblocked by ADR-0165, and the module that proves the graphics stack **composes**: the first test here holding a window, an event queue and a renderer open at once. Immediate mode with **no allocator**, which is the property worth having when the allocator is a `context` field (ADR-0061) — a UI that needs none cannot get one wrong. A click is **release inside after press inside**, never a press: returning `true` on press passes every positive test and breaks the escape hatch every user expects (press, think again, drag off, release), so four of sixteen assertions are that negative case. Hit-testing is half-open on the far edges, matching the fill convention, or a two-button row has a column that highlights both. Drawing is split from the widget call so the state machine runs with no display. **§6 is a real bug this wave's tests caught**: `is_hot` compared `ui.hot == id` and `begin_frame` sets `hot` to `NONE`, so `is_hot(ui, NONE)` answered **true** every frame — a widget that does not exist, reported as hovered. `button` already refused a zero id; the accessors did not, which is the inconsistency that survives review. General rule: a sentinel meaning "nothing" must not be askable about through the same accessor as a real value. Rejected: a retained widget tree, a call-site-derived id, a rectangle-derived id | Accepted |
| [0167](0167-image-module.md) | **`Image` — BMP, surfaces, textures, and four ambiguous names** — PLAN §8.5's image item, the last graphics-shaped one, closing W10's content. BMP because `SDL_LoadBMP_RW` is in the **base** library, so no dependency is added on top of the one ADR-0163 §1 already accepted; PNG needs `SDL_image` (version skew for a format this wave does not need) or zlib's inflate (the largest single thing the stdlib would contain, and it belongs beside a `Compress` module). Rejected too: deferring images, since a texture path that never carried a decoded image is untested and the *decode* holds the interesting failure. Surface and texture both exist because SDL's shape is real; `load_texture` frees on **both** paths, which is the leak a hand-written sequence gets wrong. `Surface_Data` is a second `#place` overlay, **and its guarantee is explicitly weaker than `SDL_Event`'s** — offset 0 there is documented in SDL's header, `w` at 16 here is only ABI — because a reader seeing both would assume otherwise. **§4: written unprefixed first, and a file importing `Window`, `Basic` and this got four E0211 ambiguous-name errors at once** (`fill`, `destroy`, `free`, `layout_is_sdl2`), so every export is now prefixed. The compiler catching it is the good outcome; ADR-0166 §7's note arrived as four errors one wave later. **Qualified imports are owed** — and rejected as a mid-wave fix, since a feature designed by an inconvenience is the wrong feature | Accepted |
| [0168](0168-comptime-poly-return.md) | **`$$` in a return type — E0290, found by auditing a table against itself** — not a wave. W10's close needed the wave table checked, and **three of its inline `[NOT DELIVERED]` markers were stale**: `it`/`it_index` (ADR-0133/0135), `[..]T` (ADR-0136/0140) and `$$T` (ADR-0137) had all shipped. The rot `AGENTS.md` warns about, one level up — those markers were *added* to correct a different rot and went stale themselves. Each was re-verified by **probe**, because PLAN and `AGENTS.md` disagreed, and the probe found an **ICE**: `$$T` as a parameter works (`valid/110`), `$$T` as a **return** checked clean and the call died with `no routine for file 0 proc 3` — the **tenth** leaked-internal-error instance, in a position nobody had written. Refused rather than implemented, because `$$` is `$` plus "the argument is a compile-time constant" and **a return has no argument**: the construct is meaningless, not unimplemented, which is the strongest case for a diagnostic. Rejected: silently treating `-> $$T` as `-> $T`, which trains a reader that the second `$` does something. The check walks the result list too, so `-> (s64, $$T)` cannot reach the ICE by one extra character. Fixture moved from `type-errors/` to `imports/invalid/` — that directory's "parses, lowers and resolves cleanly" contract was met by **moving the file, not weakening it**, the sixth such move. The lesson: a claim about the code is only as good as the last time someone ran it, and two documents disagreeing is a signal to probe rather than to pick | Accepted |
| [0169](0169-dwarf-line-table.md) | **The DWARF line table — W12's first item, from zero** — the first debug information this compiler has ever produced. A Cranelift `SourceLoc` becomes an index into a `(path, line)` vocabulary this crate owns, because Cranelift's `u32` is opaque by design; rejected: packing a `FileId` and a byte offset into it, which breaks silently on a large file. **§2 is the load-bearing decision**: `TrapLocations` gained `position()` returning a structured path/line/column and `location()` became a *provided* method formatting it, so a `.debug_line` row and a trap's `--> file:line:col` come from one resolution and cannot drift — ADR-0020 §2's one-formatter argument applied to two consumers instead of two engines. A row per statement and per terminator; a synthetic span gets **no** row rather than inheriting the previous line, which would park a stepping debugger on code that already ran. No column, deliberately: spans are per-statement, so a column would always be the first byte and a consumer would draw a caret under the wrong token confidently. DWARF 4, since 5 moves the file table where older consumers reject it. **§6 records two wrong results, both one string**: Mach-O spells it `__debug_line`, and a debug section outside the `__DWARF` segment *fails* the link with "pointer not aligned". §7: a relocation-aware `Writer`, because `EndianVec` errors on `Address::Symbol` — and the first draft recovered the symbol id by parsing `SymbolId`'s `Debug`, replaced and recorded. Verified by parsing with gimli, not grepping `dwarfdump`, and asserting rows name *real statements* — the check that would have failed on both wrong results | Accepted |
| [0170](0170-llvm-line-table.md) | **LLVM's line table — the same spans, a completely different route** — completes W12's first item, which asks for both back ends. **None of ADR-0169 is reusable**: LLVM writes DWARF itself from `!dbg` metadata, so this back end attaches a `DILocation` per statement hung from a **per-body** `DISubprogram` (LLVM rejects a location whose scope is not the enclosing function's). The two paths share exactly one thing — the `TrapLocations::position` lookup — which is why a *separate* test is right: two unrelated emitters reading one span source must agree about which lines exist, and a shared test would assert only the intersection. §2: the column **is** set here, which is not an inconsistency with ADR-0169 §4 — LLVM requires one, so the choice is the span's column or a lie, and that ADR argued against *inventing* precision rather than against passing it through. **§3 is this wave's first wrong result**: every subprogram hung off the unit's file, so the file table had one entry and `modules/Basic`'s statements were blamed on the root program — a line table naming the wrong file is worse than none, and a check on the root file alone would have passed it. `DWARFSourceLanguage::C`, since there is no `DW_LANG_Jairs`; inkwell's builder rather than the raw API for one reason — LLVM **silently** strips every `!dbg` from a module whose flags lack `"Debug Info Version"`. `finalize()` before `verify()`, or the verifier rejects temporary nodes with a message about the node rather than the missing call | Accepted |
| [0171](0171-type-dies.md) | **Type and struct-layout DIEs — and the reference that makes them exist** — W12's second item for LLVM. A `DIType` per `PoolId`, with struct member offsets from `jr_pool::field_offset` — **the same function both engines use to compile a field access**, so a debugger cannot disagree with the code about where `p.y` is; hand-computed offsets would be a second layout implementation. A `None` propagates, because a struct DIE listing *some* members shows a type whose fields do not add up to its size. The recursion terminates because **a pointer stops the walk**. **§3 is the finding**: the mapping was written and correct and `dwarfdump` showed base types and **no struct** — because **LLVM prunes a type nothing declares**, and a `DISubroutineType` is a signature, not a declaration. So each parameter gets a `DILocalVariable`, and W12's items 2 and 3 turn out **coupled** where the plan had them as separate lines. Holes are kept in the parameter DIE list, since a `filter_map` would shift every later name onto the wrong type — confidently wrong rather than absent. §2: `TrapLocations` became **`SourceInfo`** with a `symbol()` lookup, renamed rather than extended because a trait called `TrapLocations` with a `symbol()` method teaches the wrong lesson. The struct DIE is **anonymous** — the pool records no declared name and faking one from the `DeclId` prints a number no reader recognises. The test asserts **offsets**, not tags: a tag-only check passes on a struct whose every member sits at 0 | Accepted |
| [0172](0172-dwarf-locals.md) | **Local variables in DWARF — and the two boundaries writing it exposed** — W12's third item for LLVM, **half delivered, and the partition is the decision**. A `DILocalVariable` per MIR slot that stands for a source local, precomputed on the back end (the type-DIE cache needs `&mut`) and declared on the translator side (the `alloca` exists only there), meeting in a slice with **holes kept** — ADR-0171 §3's trap. §2: the lookup takes a **`LocalId`, not a span**, because the first draft keyed on `MirSpan::Local`, found `total` and **silently missed `pair`** — an aggregate's slot carries its creating expression's span, and a lookup that names *some* locals is worse than one that names none, because the gap looks like a property of the program. §3: two boundaries, both found by writing the test. **A register-resident local is invisible** — only an escaped local gets a slot (ADR-0017 §2), so a `dbg.declare` has no address; that is exactly what the value-label item is for, now *specified* rather than guessed. **An aggregate local is unnamed** — its slot carries no `LocalId`; a MIR gap, asserted **negatively** so the boundary is pinned with a message telling whoever fixes MIR to invert the line. And the declare uses the **raw `llvm-sys` record call**, because LLVM 19 replaced the `dbg.declare` intrinsic with a debug *record* and inkwell 0.9 casts it to a value and asserts `is_instruction()` — both its insert helpers panic on LLVM 21 | Accepted |
| [0173](0173-cranelift-debug-info.md) | **Cranelift's `.debug_info` — types and subprograms, by hand** — the prerequisite W12's third item needed and PLAN never listed: Cranelift had a line table pointing into a `.debug_info` that did not exist, and a variable DIE needs a subprogram to live in and a type to point at. **§1 is forced structure**: a struct's members need field names, which need the driver's per-body `SourceInfo`, available only during `define`; the DIEs can only be written once the object exists, at `finalise`. The two moments do not overlap, so a `TypeDescription` is built in the first and emitted in the second. Rejected: threading a `SourceInfo` into `finalise`, which needs a second module-scoped name resolver beside the per-body one — a new channel for a question the existing one answers. Two passes over the tree, because `DW_AT_type` is a `UnitRef` and every id must exist before anything points at one. `DW_AT_low_pc` is a **relocation**, `high_pc` a **length** (DWARF 4's form, one relocation instead of two) — wrong, and every backtrace frame resolves to the object's first function. The subprogram symbol appends to the **same** side table the line sequences use, since gimli indexes one list per writer. §4 specifies what locals still need: a stack local wants `DW_OP_fbreg` and a per-slot frame offset (probe `frame_layout`, do not assume); a register local wants `value_labels_ranges` plus `ValueLabel` emission plus `enable_value_labels` — three pieces. Test asserts **two unrelated emitters agree** about one struct, kept separate from LLVM's for ADR-0170 §7's reason | Accepted |
| [0174](0174-cranelift-locals.md) | **Cranelift's locals — and ADR-0172 §3's over-general claim, corrected** — completes W12's third item for stack-resident locals in *both* engines. ADR-0173 §4 said to **probe** the frame layout rather than assume, and the probe paid: per-slot offsets are there. A Cranelift `StackSlot` is not a MIR slot, though, and this back end creates unkeyed slots for aggregate temporaries — so a `StackSlotKey` carries the MIR index and the mapping is a fact rather than an assumption about creation order, which would work today and break silently later. `DW_OP_fbreg` against a frame base of the **frame-pointer register**, since `DW_OP_call_frame_cfa` needs `.eh_frame` this compiler does not emit; the register number is per-ABI, and an unknown architecture gets `u16::MAX` so a consumer *rejects* the expression rather than reading a real register. The test asserts the offset is **negative** — forgetting to subtract `frame_to_fp_offset` yields a location that parses and reads the wrong memory. **§3 amends ADR-0172 §3**, which concluded from one program that an aggregate local is never named. It is: one **passed by value** to a procedure is bound and named in both engines, one only field-assigned is not — the rule is about *usage*. Ninth time the habit has paid and the second this session against this project's own ADR. **A negative result from one program is evidence about that program**; generalising needs a second that differs in the suspected dimension | Accepted |
| [0175](0175-c-call-procedure-type.md) | **A `#c_call` procedure type — the blocker W11 turned out to have** — found by probing, not from the plan. `pthread_create` needs a function pointer, and three failed probes narrowed to the answer: **`expected (s64) -> s64, found (s64) -> s64`**, two identical types. `jr-pool` had *always* modelled the distinction (`ProcType.context`, ADR-0001), and **no type expression could say which** — so `ctx.rs` interned every written procedure type as Jairs with a comment explaining why that was fine, which made every `#c_call` procedure **unpassable**. Four crates changed, none gained a concept. §2: three engines hard-coded the convention at an indirect call, each with a comment saying it was safe because no such type existed; MIR and Cranelift then failed **loudly** and **LLVM did not** — it would have passed the context where C expects the first argument. **§4 closes PLAN's comptime-spawn fork on evidence**: the VM cannot marshal a procedure because C needs a machine address and an interpreter has none, so *refuse* is forced — and "grow a scheduler" is not expensive but **unreachable**, since a scheduler still needs a body to run. §5: `f :: () -> (s64) #c_call` binds to the **type**, verified by writing it, with the grammar conflict *declared* rather than given a `prec` | Accepted |
| [0176](0176-atomics.md) | **Atomics as language operations — and the passes that must not touch them** — four intrinsics, one `Rvalue::Atomic`, three engine implementations. Rejected: libatomic calls, which the comptime VM could never make (ADR-0175 §4) so every program using one would be unrunnable; and operators, which make the *ordering* invisible where an auditor must be able to search for it. **§2 is the reason it is a MIR variant**: `forward_stores` would forward a store across one, const-prop would fold a load another thread writes, and DCE would delete a compare-exchange whose *effect* is the lock — and the exhaustive-match rule turned all nine sites into compile errors that each had to be *reasoned* about. `s64` only, sequentially consistent, `add` yields the **previous** value (a ticket), `compare_exchange` is the **strong** form yielding a `bool`. The interpreter implements them non-atomically, which is *correct* because nothing there can spawn — and wraps rather than traps, matching the machine. **§6: `file_consts`'s early-out is a feature list nothing enforces, and this is the third time it has bitten** — the comment above it already records the previous occurrence, ending "Found by running the feature's own probe" | Accepted |
| [0177](0177-thread-and-memory-model.md) | **`Thread`, the memory model, and W11 closed** — the last of the twelve waves. `spawn`/`join`/`joinable` plus a spin lock, as a *binding*: rejected a Jairs-shaped `spawn(f: () -> void)` because the trampoline needs a `#c_call` procedure to be, one level down. A **spin lock rather than a mutex**, because `pthread_mutex_t` is 64 opaque platform-sized bytes and this language cannot spell that without hard-coding N per platform — the wall `sockaddr_in` hit; it **yields on every failed attempt**, which is not politeness but what stops a single-core dead-lock. §3 writes the **memory model** down, and its data-race clause is *measured*: the same three-thread program with a plain `+ 1` produced **1000 instead of 3000**. §4 names what is not delivered and why each is separate — chiefly a **per-thread shadow stack**, which §8.3 had put in this wave and which needs thread-local storage in both back ends, so a trap in a spawned thread still stops the program but may name the wrong frames. The test runs the binary **five times**, because a race that fails one run in three passes one run in three | Accepted |
| [0178](0178-refused-body-stub.md) | **A refused body gets a trapping stub — the declare phase's promise, kept** — a latent defect found while auditing W11, not a planned wave. `jr check` handles a file-scope mutable variable *well*: E0245, with a note saying **"calling it is an error; leaving it uncalled is not"**. `jr build` **panicked** — `function "jr$0$0" with linkage Export must be defined but is not`, exit 101, a mangled symbol and nothing connecting it to the diagnostic already printed. Phase 1 declared the procedure `Export`; phase 2 *skipped* the refused body under a comment that was reasoning about **diagnostics** in a place whose problem was **linkage**. The bytecode VM got it right the whole time, which is what made it a defect rather than a shared gap. So a refused body is now *defined* as a stub carrying the declared parameters and terminating in `Unreachable::Refused` — E0245's own note made true at run time. Rejected: failing the build (breaks the second half of that promise); skipping the declaration too (relocates the panic into `Undeclared`); gating E0245 on reachability (`AGENTS.md` records why that was declined once — it *would have masked* ADR-0120's four defects). §2: its own variant, not `Unreachable::Trap`, because sharing one printed **"reached a deliberate trap"** for a program that deliberately did nothing — six sites became compile errors and each answer is argued. **§3 is the second finding**: the registry test guarding `TrapKind::ALL` asserted `len() == 11` and **worked by luck**; replaced with an exhaustive match, it immediately found that **four kinds were never in `ALL`** — and `ALL`'s doc claimed a driver loop that *does not exist*, which is what let the omission stand. Those four were therefore never checked by `reasons_are_distinct`, whose whole purpose is that no two kinds share a sentence, since the corpus differential compares rendered messages | Accepted |
| [0179](0179-qualified-imports.md) | **Qualified imports — `Simp :: #import "Simp";` and `Simp.name`** — the wall the graphics restructure needs removed, measured rather than argued: `Window` and `File` both export `open`, so **a graphics program that loads a file was unwritable** (E0211), and three modules in the tree already carry workarounds for it — `UI` renamed its own sentinel to dodge flat names, `Image` is fully prefixed after four collisions in one wave. §1: the aliased form needs **no grammar rule**, because a constant whose value is a directive expression already parses — recognised in lowering by the directive's name, like `#bake_arguments`. §2 is one `continue`: an aliased import merges nothing, so the ambiguity cannot arise. **§4 is the decision the group turns on, and it overrules the plan.** The plan said resolve `Res::Imported` onto the `Expr::Field`; counting the cost killed it — sema reads a callee as an `Expr::Name` at a dozen sites and MIR at seven more, and a construct half-represented on the lowering path is this project's first named failure mode. Carried on the *name* instead (`Expr::Name { module: Option<Symbol> }`), **nothing downstream learns anything**: four construction sites became compile errors, seven MIR patterns needed `module: _`, and no MIR logic changed. **A second code was drafted and refused** — E0293 for "the alias is not an import" has *no reachable condition*, since a local of that name makes the access an ordinary field (ADR-0014 §3, enforced by where the check sits) and a colliding declaration is already E0200; a code with no condition reads as a promise that something is checked. §5: a qualified *type* is a new `TypeRef` variant with **six** real match sites, not the seventeen the plan estimated, and its refusal is sema's E0212 rather than E0292 because the two positions are answered by different crates — the asymmetry the unused-import warning already lives with. Both spellings intern to one `PoolId`, verified by a program that declares a local one way and passes it the other. The formatter dropped `W.Event` to `W` on the first attempt — the **thirteenth consecutive wave** `jr fmt` has had to learn a construct, and again the unsound direction. |
| [0180](0180-target-os.md) | **The target operating system as a compile-time value** — Group B, and the compiler had **no notion of an OS anywhere** before it: its whole notion of a target was `TargetLayout`'s two numbers, and the consequence was recorded in the *library* (`modules/Time`'s `CLOCK_MONOTONIC :: 6`, "a real portability gap … named rather than hidden"). Rejected item-level `#if` (it reshapes the item tree, ADR-0072 §5, and every case here only needs a *number*) and a per-OS library **name**, which is not merely unimplemented but **circular**: library resolution happens inside `file_signatures` and `file_consts` depends on it, so a computed `#system_library` operand cannot be evaluated before the library must be known. §1 declares the enum in `modules/Basic` for ADR-0075 §2's reason — a caller must be able to *name* the type. **§2 overrules the plan**: the value is a `cfg!`-derived constant beside `TargetLayout`, not a `BuildConfig` field, because invalidation for a value that *cannot change within a process* buys nothing (no `--target`; `jr-link` shells to the host `cc`, `jr-vm` resolves symbols from its own image) and the cost was **measured at ≈50 `file_signatures` call sites across six crates**; the salsa input is owed the day `--target` exists. **§3 names the plan's wrong cause and the right one**: the plan blamed a one-arm asymmetry in `thunk.rs`, and fixing it changed nothing because **nothing had put a value in the channel** — the fold happens in the *signature* phase (a named item's initialiser is typed there and the check phase deliberately does not revisit it) and `SignatureOutput` had no `folded_calls` field, so the value was computed and thrown away. Closing it made `Window.LAYOUT_IS_SDL2` and `Image.SURFACE_LAYOUT_IS_SDL2` constants — two documented workarounds gone — and `file_consts`'s unenforced feature list needed a **fourth** entry for a **third** distinct reason. **§4 is a second cause found by probing the neighbour**: `N :: size_of(s64);` still failed, because `resolve_all` walks the top-level arena **flat** and visited `s64` as an expression in its own right before reaching the call that makes it a type argument — the map ended up right and the diagnostic was already pushed. §5 closes two `#system_library` holes that type-checked clean and emitted no `-l` (E0293, *not* the E0294 the plan allocated, because Group A spent only one code). §6: `switch` passed `None` for a poisoned scrutinee, which **routed around** `check_bare_member`'s own `ERROR` guard — caught by the harness whose contract is "stay silent on poison". |
| [0181](0181-per-os-clock.md) | **A per-OS value in the standard library — `CLOCK_MONOTONIC`** — Group C, deliberately one item: the end-to-end proof of ADR-0180 on the one place in the library that already documented the problem. The `#if` the old comment asked for was **not built and is not needed** — `os()` is a *value*, so the selection is an ordinary procedure a `#run` evaluates, the idiom `modules/Random`'s `GOLDEN :: #run golden_seed();` already used. Windows gets **0** as a decision rather than a placeholder: it has no `clock_gettime`, so there is no correct number, and `0` is `CLOCK_REALTIME`'s — a real reading if the binding resolves, and otherwise the `-1` the module already reports. Rejected: inventing a plausible-looking constant for a call that cannot happen, which is exactly what the old `6` was one platform over. **The verification tests the clock, not the number**: a wrong id makes `clock_gettime` fail and `monotonic` return **0 twice**, which a "second is not before the first" comparison accepts happily — so the corpus file exits on a *zero* reading. |
| [0182](0182-simp-renderer.md) | **The graphics modules restructured onto a Simp-shaped API** — Groups D and E, the wave the two language groups existed for. Three modules become five on `SDL_RenderGeometry` instead of `SDL_RenderFillRect`, and Simp's own shape is **verified from primary sources**: the Jai wiki says *"SIMP has a GL backend"* — single-backend, with all the per-OS code in `Window_Creation` — so the thing to imitate is one portable library with the platform work quarantined, not three backends behind one API. `SDL_Vertex`'s 20 bytes and its three offsets were **measured with a `cc`-compiled `offsetof`**, and `SDL_RenderGeometry` was **called from Jairs before a line of the module existed**. **The plan was wrong in three places, each found by writing the thing.** §1: it said the renderer and the batch are *module-level state* — **Jairs has none**, a file-scope `var` is E0245, probed for a scalar and an array, which made two of the five items unbuildable as written. The answer is not a compiler feature but `modules/UI`'s own pattern: a caller-owned struct passed by pointer. Rejected implementing file-scope variables (a `.data` section, static init, three engines — a *language* wave this one would have been blocked behind), and the explicit version is the better API anyway, since two windows can have two renderers where a global cannot. §3: the plan put `get_render_dimensions` in `Window` and said it binds `SDL_GetRendererOutputSize` — which needs the renderer `Window` no longer has, so **it could not have compiled**; it is `Simp`'s, as Jai has it, and `Window` gets `get_window_size` because on a high-DPI display the two answers differ. §2 corrects every `#foreign` width to C's, which the old bindings got wrong in a file whose own `Rect` docs explain why it matters. §5's cost is named: `UI`'s outline is **four thin quads**, because `SDL_RenderGeometry` has no line primitive — bought back by body and outline being different colours *in one batch*, and every assertion in the UI test is unchanged, which is the only real check that a migration preserved behaviour. Six integration tests; the rotated textured quad is the one that proves the new API says something the old one could not. |
| [0183](0183-framework-link-form.md) | `jr-link` learns a second argument form — `-framework` | The plan's blocker was not the one it named: a perfect per-OS name mechanism would still have emitted `-lOpenGL`, which does not link. `LinkKind` is interned *into* the library value, so `#system_library "X"` and `#framework "X"` are different values, and there is no inference and no fallback between the forms. |
| [0184](0184-file-scope-insert.md) | `#insert` at file scope — comptime code that generates declarations | Wave A: one missing arm in the file-scope directive dispatcher was the whole gap. Generated items go straight into the file's arena, so a generated declaration is an ordinary one; signatures are recomputed over the expanded tree, correcting a comment that had expired. `modules/GL` selects a library *and* its link form per OS in ordinary Jairs. |
| [0185](0185-string-literal-place.md) | A string literal's `.data` and `.count` lower | One missing arm in one guard: `field_place` spills an aggregate receiver with no place into a slot, and `StringType` was not in the list — so `"literal".data` refused the body while `s := "literal"; s.data` worked. Found in the first SDL call of a GL probe. |
| [0186](0186-file-scope-globals.md) | File-scope mutable variables | `PlaceBase::Global`, a program-lifetime memory root, in all three engines. Built because Jai's real `Simp` API has no state argument. Nine `jr-mir` sites had to decide what a global means; the tenth — `forward`, where forwarding a store is unsound — was a `let-else` and **did not** fail to compile, which narrows this project's trust in the exhaustive-match rule. |
| [0187](0187-simp-on-opengl.md) | `Simp` and `Window` on Jai's real API, over OpenGL | Signatures recovered from copies of Jai's own module vendored in two projects and diffed. Eight signatures were wrong, and two not cosmetically: the coordinate origin was mirrored and every call carried a state handle. GL 2.1 / GLSL 1.20, measured. ADR-0182 §1's "a caller-owned renderer is better" is withdrawn as a limitation described as a choice. |
| [0188](0188-two-stale-claims.md) | Two stale claims in the compiler, each costing a working program | A constant's value is keyed by `ItemId` and a computed `#insert` renumbers those — the hazard was documented and fixed in the *adjacent* map. And sema said it could not reach an imported signature; it could, so a default argument silently did not apply across a module boundary. |
| [0189](0189-print-formatted.md) | `print` with `%` placeholders, and four compiler defects behind it | The first caller to compose the `..Any` variadic, `Type_Info`, `Any` erasure and a file-scope global. Amends ADR-0076 §1: an argument now describes *itself*, because otherwise a pointer is unprintable. Three shipped `imports.is_empty()` guards hid `modules/Basic`'s own library types from itself — the fourth proxy-guard of this shape. And the **inliner** creates cross-file global references, which ADR-0186 §3 assumed impossible while deliberately making it so. |
| [0190](0190-typed-constants.md) | Typed constants — `name : T : value` | The highest-value small item, owed for three stretches. Twenty `cast(u32, X)` in `modules/GL` are gone, and the one that survives now *means* something — `internalformat` is a `GLint` where `format` is a `GLenum`. Carried as a **field** on `ConstValue::Expr` rather than a new variant, so all thirty existing sites became compile errors instead of silently not matching. The formatter dropped the annotation (fourteenth wave in sixteen) and the first fix mistook a `struct` *value* for one. |
| [0191](0191-pointer-type-argument.md) | A pointer type as an intrinsic's type argument | ADR-0189 §2 made a pointer the common thing to find in an `Any` and left no way to read one back — `any_as(a, *Point)` was E0261. One arm in `described_type`, the function every intrinsic asks, so `size_of(*T)` and `type_info(*T)` fall out too. Found because a migrated test worked around it. |
| [0192](0192-type-of.md) | `type_of(x)` — the type of a value | 14 uses in real Jai code. One arm in `described_type`, so four intrinsics gain it together. The obvious fix — clearing the type-position flag — was unnecessary *and* worse, found by probing the refusal case; the real fix turned MIR's five-entry exemption list into a **property** (the call denotes a type). Poison propagates, which is what makes it work inside a `$T` template. |
| [0193](0193-reflection-members.md) | An enum's members, a view's stride, and a structural type's spelling | Three of ADR-0189 §6's four print gaps, none of which needed the `*Type_Info` table that section prescribed — and the naive version of that table **diverges** on `Node :: struct { next: *Node; }`, which is why ADR-0077 chose ids. A view's `element` was also never populated, invisible until the stride arrived beside it. Names compose and terminate because a declared type answers by name. |
| [0194](0194-array-literals.md) | `T.[a, b, c]` — fixed array literals | The most used construct real Jai code has and Jairs lacked (39 occurrences). **Naming the element type answers all three questions ADR-0039 §6 deferred** by construction: the length is the count, elements are ordinary expressions, and the named type is each element's *expectation*. Carried as an expression, so `Point.[…]` and `type_of(x).[…]` need no code. Spilling a value sequence for `for` also fixed `for x: f()`. The formatter deleted the whole literal — fifteenth wave, and needed **two** emitter entries. |
| [0195](0195-build-script.md) | A build script written in Jairs | `jr build build.jr` compiles a Jairs program, runs it in the VM, and performs the compilations it asked for. Jai's `#run` model could not work — comptime code may call no `#foreign`, so a `#run` cannot read a file — and Jai's build power turned out to be the *standard library* rather than the compiler API. `#compiler_library` reuses the `#foreign` form, so no grammar, no HIR node, no MIR variant, no back-end change; the dispatch keys on `LinkKind::Compiler` because a library *name* is forgeable. Shelling out is a driver-side spawn, because no type distinguishes `argv` from `strtod`'s out-parameter. |
| [0196](0196-comptime-can-allocate.md) | Compile-time execution can allocate, print, and declare a build | Amends ADR-0195 §2, whose claim that "a `#run` cannot allocate" was **wrong**: `ffi.rs` has served `malloc` from the VM's own region since ADR-0061 and the refusal was keyed on the `#foreign` *declaration* rather than on whether a host is reached. Jai does the same — its comptime `context.allocator` is an ordinary Jai module, and no allocator is `#compiler`-provided (read from five vendored `Runtime_Support.jai` copies). Getting from there to comptime `print` uncovered four more expired claims, including ADR-0053 §2's cycle reason: `file_consts`' first statement already calls `checked`. `Compiler.build` in a `#run` is refused because salsa says "Cannot change database mid-query"; `request_build` declares instead, which is what `add_build_file` does. |
